# Snipflow Page 架构


## 1. 职责与设计约束

Page 是 Snipflow 的浏览器客户端，依据 `../README.md` 的原始对象 API 传输文本和任意文件，不重复实现后端存储与鉴权业务。前端按 TXT、MD、PNG、JPEG、JSON 等具体类型展示内容块，类型体系可扩展，不使用固定三类枚举限制文件。

| 维度 | 约束 |
|------|------|
| 整体风格 | 简约、精致并具有高级感；以克制的层次、细腻的反馈和稳定的节奏建立品质感 |
| 页面结构 | 发送与接收以单输入区和内容块为中心；仪表盘仅通过 URL 进入，以瀑布流展示 |
| 视觉表达 | 通过背景、色彩、流动与动效表达传输模式和状态 |
| 交互体验 | 以清晰的可发现性、即时且可理解的响应、自然的过渡和可撤销的反馈为优先；点击、触摸、键盘与辅助技术保持同等能力 |
| 响应式适配 | 布局、间距、内容密度和交互热点由容器、内容和可用视口动态决定，不为单个 UI 块设定不可变的像素尺寸 |
| 配色 | 浅粉、浅蓝、浅绿与黑白组合；色值及语义映射按第 7 章基线 |
| 开发联调 | 通过前端开发服务代理访问 Worker，不修改后端 CORS |
| 正式部署 | 前后端同源部署，路由分流按部署环境配置 |

## 2. 技术栈

### 2.1 主表

以下为已确认选型。除非明确记录兼容性冲突，依赖默认采用实施时可用的最新版本；开发时复核运行时、浏览器、插件和 peer 依赖的兼容性，并用 lockfile 固定实际安装结果。发现版本冲突时，必须在依赖清单、构建记录和本章对应条目中显式标出冲突、受影响能力及取舍，不以强制升级或忽略 peer 警告掩盖问题。

版本冲突标记统一写作“兼容性冲突：冲突对象 / 受影响能力 / 采用的兼容方案 / 后续复核条件”；没有该标记时，不额外标注版本。

| 层级 | 选型 | 约束 |
|------|------|-------------|
| 应用形态 | React SPA | 客户端渲染 |
| 开发与 CI 运行时 | Node.js LTS | 与部署运行时及工具链兼容 |
| 包管理器 | pnpm | 与 lockfile 格式及 CI 镜像兼容 |
| 语言与模块 | TypeScript、ESM、TSX | strict、noUncheckedIndexedAccess、exactOptionalPropertyTypes |
| UI 框架 | React、React DOM | 函数组件与稳定现代 API |
| 构建工具 | Vite、@vitejs/plugin-react | 插件与构建器保持同一兼容链 |
| React 编译器 | babel-plugin-react-compiler | 采用稳定 Babel 实现 |
| 编译器集成 | @rolldown/plugin-babel、@babel/core | 使用 reactCompilerPreset；实施时按兼容矩阵解析配套依赖 |
| 局部状态 | React useState / useReducer | 组件内部状态 |
| 共享界面状态 | Zustand | 组件共享界面状态 |
| 服务端状态 | @tanstack/react-query | 管理服务端状态 |
| 前端路由 | @tanstack/react-router | 认证入口及三个平级功能页面 |
| 控件底座 | @base-ui/react | 提供无障碍控件基础能力 |
| 视觉组件 | 项目自定义 React 组件 | 基于 Base UI 与语义化 HTML |
| 样式 | Tailwind CSS、@tailwindcss/vite | 插件与核心版本保持兼容 |
| 设计令牌与背景样式 | CSS 自定义属性、原生 CSS | 新 CSS 特性按目标浏览器支持采用 |
| 图标 | lucide-react | 统一图标来源 |
| 表单 | React 原生表单 | 与 Zod 配合 |
| 运行时校验 | Zod | 校验控制响应 JSON、索引及 Header 派生元数据，不将对象正文当作 DTO |
| 动效 | Motion、CSS transitions / keyframes | motion/react；支持 reduced-motion |
| HTTP 与取消 | fetch、AbortController | 浏览器原生 API |
| 文件与剪贴板 | File、Blob、ArrayBuffer、TextEncoder / TextDecoder、ClipboardEvent | 原始字节传输；显式块转换中的文本编码与解码见 4.4 |
| 文件选择与拖放 | react-dropzone | 仅负责文件接入，不承担上传请求 |
| 文件特征检测 | file-type | 受支持范围及安全限制约束 |
| XML 结构校验 | fast-xml-parser | 仅在可终止 Worker 中使用 XMLValidator，不展开 DOCTYPE / ENTITY，不依赖 Worker 的 DOMParser |
| 下载与图片预览 | Object URL、download、img | 释放 Object URL |
| Markdown 预览 | react-markdown、remark-gfm | 已确认渲染需求；实施时核查配套依赖兼容性，不解析原始 HTML |
| 日期与数值格式化 | Intl | 浏览器原生 API |
| 开发代理 | Vite server.proxy | 转发 Worker API 请求 |
| 生产 API 访问 | 同源相对地址 | Bearer Token 运行时提供，不写入源码或构建产物 |
| Token 缓存 | localStorage | 保存 Token 与最近续期时间；离开功能页后默认 30 分钟过期，可部署配置 | 
| 静态检查 | oxlint | 配置见 .oxlintrc.json；与 TypeScript 严格检查配合 |
| React 检查规则 | Oxlint 原生 react 规则 + eslint-plugin-react-hooks | 同名 recommended 规则优先使用原生实现，config / gating 通过 jsPlugins 别名 react-hooks-js 加载 |
| Query 检查规则 | @tanstack/eslint-plugin-query | recommended 规则通过 Oxlint jsPlugins 执行，不另跑 ESLint |
| 格式化 | Prettier、prettier-plugin-tailwindcss | 配置见 .prettierrc.json 与 format 脚本 |
| 单元测试 | Vitest | 阶段一接入，配合 Testing Library 与 jsdom |
| 组件测试 | @testing-library/react、@testing-library/user-event | 组件交互测试 |
| DOM 测试环境与断言 | jsdom、@testing-library/jest-dom | 补齐声明的配套依赖 |
| API 模拟 | MSW | 模拟网络边界 |
| 浏览器测试 | @playwright/test | 浏览器流程测试 |
| 可访问性测试 | @axe-core/playwright | 浏览器可访问性检查 |
| 部署工具 | Wrangler | 静态 ASSETS 与现有 API 的 Service Binding，配置见 wrangler.example.jsonc |

静态检查统一执行 `oxlint --deny-warnings`，配置集中在 `.oxlintrc.json`。升级或切换版本后必须验证规则实际报错。它支持加载 ESLint 格式的 JS 插件，不要求单独运行 ESLint 或使用 typescript-eslint parser。JS 插件能力仍可能处于 alpha，插件的 peer / 间接依赖可能仍安装 eslint 包，但项目不将其列为直接依赖，也不建立第二条检查链。若依赖与插件、Node.js 或构建环境发生冲突，记录冲突并选择经过验证的兼容版本，同时说明这是例外而非默认版本策略。TypeScript 严格类型检查继续由 tsc 执行，不把 JS 插件兼容性等同于完整类型感知支持。

### 2.2 备用表

备用依赖不属于当前安装清单。只有已确认需求超出主表能力并完成兼容性核查后才引入；引入时遵循同一版本选择与冲突记录规则；替换类方案不得与同职责依赖重复。

## 3. 页面结构与交互

### 3.1 发送页面


#### 3.1.1 认证与路由

| 路由 | 页面 | 入口与访问规则 |
|------|------|----------------|
| /auth | Token 认证 | 有效本地缓存可直接进入；无缓存或已过期时输入 Token，通过验证后保存 |
| /send | 发送 | 默认功能页面，与接收通过背景融合控件或手势切换 |
| /receive | 接收 | 与发送平级，不使用导航栏切换 |
| /dashboard | 仪表盘 | 仅通过指定 URL 进入，不在其他页面显示入口，不加入手势切换序列 |
| / | 入口 | 未认证进入 /auth，已认证进入 /send |

三个功能页面平级，无统一设置页。仪表盘隐藏入口不替代鉴权；直接访问先检查本地 Token 缓存，缺失或过期则转认证页。手动验证成功后恢复站内目标，网络异常不当作凭据错误。所有 API 请求携带 Bearer Token。路由支持刷新、前进与后退；URL 不包含 Token、正文或附件原文。账户入口只提供退出，不建立设置页或草稿恢复系统。

#### 3.1.2 发送与接收切换

采用固定空间关系：发送在左，接收在右。无导航栏、页签或发送内容类型切换按钮。

| 方式 | 交互定义 |
|------|----------|
| 背景融合控件 | 发送页右侧、接收页左侧各保留一个低对比度方向图标；悬停或聚焦提高对比并显示目标名称；单击切换路由 |
| 触屏滑动 | 在空白背景向左滑进入接收，向右滑回发送；达到阈值才提交，否则回弹 |
| 鼠标拖动 | 仅空白背景响应水平拖动，采用相同的提交与回弹规则 |
| 键盘 | 方向控件可聚焦并通过 Enter / Space 激活，不劫持文本编辑方向键 |
| 过渡 | 内容区沿切换方向轻移并交接淡入淡出，背景色场连续插值；提交后同步路由与目标焦点 |

拖动范围排除输入框、内容块、弹层、滚动区域和浏览器边缘返回手势区域。纵向滚动、文件拖入与页面切换分别处理；取消手势不改 URL，连续导航不重复压入历史记录。浏览器前进、后退复用过渡，不再创建历史记录。

方向控件始终可辨认，具有可访问名称，不仅依赖手势发现。reduced-motion 下使用即时切换或短淡入。过渡节奏、手势阈值和色值在视觉规范与浏览器原型验证中动态确认。

#### 3.1.3 公共内容块

三页使用同一内容块组件体系，以具体 fileType 标识 TXT、MD、PNG、JPEG、JSON、PDF 等类型，并允许后续扩展。文件类型、正文或附件语义、预览能力分别建模；不再以 text / image / file 三种 displayKind 统领所有行为。API 仍只使用原始正文、MIME 与文件名，不新增 type 字段。

| 项目 | 约束 |
|------|------|
| 静止外观 | 使用类型图标和简短类型标识，如 PNG、JPG、MD、JSON；类型名称可常驻，正文、完整文件名和图片内容仍默认隐藏 |
| 尺寸 | 使用由内容、容器和可用视口共同决定的稳定尺寸策略；hover、加载、倒计时不撑大块体，也不要求固定像素档位 |
| 类型表面色 | 使用 7.4 的独立 block-* 浅色组合，不复用发送浅蓝或接收浅粉背景令牌；同类型跨页保持一致 |
| 内容信息 | 默认隐藏，在统一详情容器内展示元数据与适用预览，选项在详情内展开，不另开预览层 |
| 桌面交互 | hover / focus-within 仅显示详情意图与右下角快捷区；主体进入详情，快捷区执行上下文主操作 |
| 触屏交互 | 轻触主体进入详情，右下角热点执行快捷操作，长按进入详情；操作区域互斥 |
| 手势互斥 | 长按、拖动和文字选区结束后不得再触发单击；弹层点击不得透传到块体或背景 |
| 内容预览 | 由详情页根据 fileType 选择：位图按比例显示，MD 渲染 Markdown，源码类型以纯文本显示；不支持时展示摘要，不执行附件 |
| 键盘语义 | 主体与右下角快捷区分别可聚焦，不嵌套交互按钮；Esc 关闭详情后恢复焦点 |
| 状态反馈 | 区分准备、请求中、成功、失败及过期；动效不是唯一提示，辅助技术可获知名称和操作 |

统一规则：主体进入详情；右下角快捷区在发送页执行发送、接收页按类型复制或下载，仪表盘不设置传输快捷操作。

#### 3.1.4 三条准备路径

```text
文本输入 -> 完成确认 -> 只读文本块 ---------------------+
附件拖入 / 粘贴 / 选择 -> 本地读取与校验 -> 附件块 -----+-> 块上选项 -> 最终发送
raw 文本 -> 自动推荐 / 文本块手动转换 -> 附件块 -------+
```

| 路径 | 第一阶段 | 成块后 |
|------|----------|--------|
| 文本 | 明确确认输入完成，输入框收拢为文本块 | 不可直接编辑；详情页选项提供全文查看、重新编辑、手动转为附件 |
| 附件 | 文件本地读取与校验成功后形成附件块，无法识别类型时显示未知 | 预览、摘要、转为文本、移除或替换；成块后类型只读 |
| raw | 自动推荐或手动进入文本转附件编辑界面，确认类型和解释方式后转换 | 形成同一种附件块；转为文本时可恢复转换前原文 |

文本输入的高度与长文本展开规则如下；此处的“下拉框”指向下展开内容的控件，不是选项下拉菜单，也不是输入框内部滚动条。

| 阶段 | 行为 |
|------|------|
| 多档增长 | 输入达到当前容量阈值时平滑扩展到下一档；档位由内容密度、容器宽度和可用视口动态计算，之后可继续延伸到可用屏幕高度 |
| 铺满屏幕 | 上限按当前可用视口扣除顶部、底部操作区和安全留白计算；软键盘占用空间时相应缩短，不把某个固定高度当作所有设备的最终上限 |
| 首次下拉 | 超过首屏容量的内容暂时折叠，在已展示文本底部出现向下箭头和剩余内容提示；点击后在页面文档流中展开下一段 |
| 连续展开 | 每段不超过一屏可用高度；仍有剩余时在新段末尾再次显示展开控件，逐次点击直到全文展示；使用页面纵向滚动，不给输入框增加纵向滚动条 |
| 输入与粘贴 | 大段粘贴后保留完整原文，仅折叠显示；键入使光标进入下一段时自动展开对应段并保持光标可见，不能隐藏正在编辑的位置 |
| 数据完整性 | 分段只影响展示，不拆成多个 snip，不裁掉未展开内容，不增加分隔换行；确认、raw 检测和发送始终使用完整原文 |
| 重新编辑 | 还原完整原文并重新按视口计算档位与折叠段；完成确认后仍形成一个稳定尺寸的只读文本块 |

普通文本编辑阶段只显示完成确认；自动推荐可引导用户进入转换前编辑界面，类型、文件名和解释方式在该界面设置。发送选项和最终发送在成块后提供。重新编辑再次确认后回到同一草稿，保留 key / TTL；正文与附件的名称、MIME 随当前内容形态适配。附件成块仅表示本地准备完成；三条路径都在最终 POST /snip 时发送原始字节，key、TTL、文件名等通过 Header 提交，不提前上传。

文件拖入时显示明确的接收区域与文字反馈，区分可添加、需要确认替换与不可接收状态；离开、取消和放下后清理反馈，跨子元素不闪烁，不触发页面切换或浏览器打开文件。不可接收时解释原因，普通文字拖动不显示文件提示。完整视觉与辅助技术反馈在阶段十收口。

首版每次准备一个块，不支持混合内容、多文件或目录。新内容不得静默覆盖已有块；同一粘贴事件兼有文件与文本表示时优先文件，不生成重复内容。切换页面保留当前认证会话的发送草稿。

#### 3.1.5 文本块与附件块的双向转换

raw 指用户复制的文件原始内容，不预设为 Base64。明确文本结构或解码后的魔数仅用于推荐，确认前始终保留原文，规则见 4.4 节。发送草稿仅有“文本转附件”和“附件转文本”两种块转换操作；自动推荐是前者的入口，“恢复原文”是后者在存在转换前原文时的表现，不另建第三种操作或附件子类型。

| 路径 | 触发 | 结果 |
|------|------|------|
| 自动推荐 | 输入稳定后检测到明确文本结构或可靠二进制候选 | 进入文本转附件编辑界面，预填格式、名称及解释方式，展示输出大小，用户确认才生成附件 |
| 文本转附件 | 文本块在详情选项选择“转为附件” | 无论检测是否成功均可进入转换前编辑界面，设置类型、名称与解释方式 |
| 拒绝、取消或失败 | 推荐被拒绝，或转换未完成 | 保留操作前的编辑状态或内容块、完整内容及 key / TTL，不强制进入另一状态 |
| 附件转文本 | 任一本地待发送附件在详情选项选择“转为文本”；有转换前原文时可标注“恢复原文” | 有原文则恢复该原文，无原文则将 Blob 解码为文本；进入文本编辑，完成确认后形成文本块，保留 key / TTL，不新增第二个块 |

文本转附件编辑界面提供文件名、文件类型和解释方式，默认原文 UTF-8，也可明确选择 Base64、Base64 Data URL 或十六进制解码。确认前显示实际解释方式与输出字节数；解释失败时保留原文并报错，不静默回退。扩展名与检测冲突时不改变实际格式或绕过预览限制。类型与 MIME 在附件成块后只读；若转换不合预期，先转回文本，再重新设置转换参数。

同一原文拒绝自动推荐后不重复弹窗；输入变化使旧候选失效。检测、转换和预览均不发起上传，成块后仍需最终发送。首版类型清单、名称规则及解码边界见 4.4 至 4.5 节。

#### 3.1.6 块上选项与最终发送

| 操作 / 状态 | 交互定义 |
|-------------|----------|
| 发送选项 | 成块后在详情内展开 key、有效期、附件文件名及适用的编辑 / 转换操作；附件类型与 MIME 只读，改名不能改变类型，类型设置仅在文本转附件前提供，不建立全局设置 |
| 默认值 | 随机 key、24 小时有效期；允许永久保存 |
| 选项归属 | key / TTL 绑定当前草稿，重新编辑及双向转换保留；转为文本后按无名正文发送，附件名称与 MIME 不继续作用于文本；成功后新建恢复默认 |
| 最终发送 | 通过块右下角快捷区执行；主体单击只进入详情，详情内同样提供发送 |
| 请求中 | 锁定当前块、转换和选项，防止重复提交；打开详情、展开选项或查看预览本身不触发发送，详情内明确的发送按钮遵循相同锁定规则 |
| key 冲突 | 保留内容，修改 key 或明确确认后设置 X-Snip-Overwrite: true，不自动覆盖 |
| 成功 | 块体进入成功状态，相邻凭证区显示服务端 key；凭证复制不改变主体详情行为 |
| 继续发送 | 成功后使用明确的新建按钮清除已提交块，恢复默认选项 |
| 失败或结果未知 | 保留块和选项，区分明确拒绝与未确认结果，不盲目重发 |

复制反馈以实际剪贴板结果为准；保存成功不等于对端已接收。跨页请求、取消和结果未知的处理见状态章节。

#### 3.1.7 附件类型确定与成块约束

直接导入和由文本转换得到的附件使用同一种附件块，统一持有 Blob、文件名和类型信息，具有相同的预览、转为文本、移除和替换能力。转换前原文只是可选的本地恢复信息，不构成另一种附件类型。

| 场景 | 规则 |
|------|------|
| 直接导入文件 | 保留原始 Blob，依据文件名、MIME 和可靠检测确定类型；无法可靠识别时统一显示未知类型，仍可发送和下载，不要求用户选择类型 |
| 文本转附件 | 在转换前的编辑界面设置文件名、目标类型和解释方式；自动推荐也在此确认，批准后才生成附件块 |
| 转换前类型控件 | 按文本、图片、文档、归档等分组，可搜索类型或扩展名；使用 Base UI 控件，只出现在文本转附件的编辑界面 |
| 字节与类型 | UTF-8 原文保存或明确的 Base64 / Data URL / hex 解码决定字节；类型选择不执行格式转码，已识别 PNG 不能声明为 JPEG，普通文本不能靠改名成为 PDF |
| 自定义格式 | 转换前允许设置安全扩展名与适用 MIME；无法验证时标明未验证，不据此开放不安全预览 |
| 转换确认 | 一次性生成 Blob、名称、MIME 和类型标识；保留 key / TTL，不上传，不留下标签与正文不一致的中间状态 |
| 已形成附件块 | 文件类型与 MIME 只读，不提供类型下拉或类型修正入口；可修改安全文件名，但不得借扩展名改变类型、MIME 或字节，与已确定类型冲突的名称不应用 |
| 转错类型 | 使用附件转文本操作；有转换前原文时恢复原文，随后重新设置转换参数并生成附件，不直接修改现有附件类型 |
| 附件转文本 | 所有本地待发送附件都提供；有转换前原文时恢复原文，否则将 Blob 按 UTF-8 解码为文本，二进制也不阻止转换，规则见 4.4 |
| 提交中或已提交 | 禁止两种块转换及内容修改；接收和仪表盘对象保持只读，不因复用附件块获得发送草稿编辑能力 |

最终仍以当前附件 Blob 的原始字节作为 body，通过 Content-Type 与 X-Snip-Filename 提交。主体点击或长按打开统一详情，详情直接展示适用预览和可展开选项；右下角快捷区直接执行上下文主操作。预览支持范围统一以 4.4 为准，内容转换或替换时释放旧预览资源。

直接导入时将 ZIP 的 application/x-zip-compressed 别名规范为 application/zip；浏览器 MIME 仅是提示，不是字节检测。对 .ts 的视频 MIME 歧义，仅在无其他二进制签名且全文可严格解码为无二进制控制字符的 UTF-8 文本时使用源码 MIME；否则保留视频声明；有可靠二进制签名时沿用签名分类，无可靠签名时按未知附件处理。FILE 仅预留给转换前明确选择的自定义格式，直接导入的未识别扩展名统一使用 UNKNOWN。

### 3.2 接收页面


#### 3.2.1 输入与结果

```text
输入 key -> 提交获取 -> 解析 -> 文本块 / 附件块
                              -> 主体进入详情
                              -> 右下角快捷区复制 / 下载
                              -> 点击空白背景返回 key 输入
```

接收页只保留一个 key 输入框和明确的获取按钮；成功后输入区转为内容块，点击空白背景返回输入。

| 内容语义与具体类型 | 右下角快捷区 | 详情页与详情页选项 |
|------|----------|----------|
| 无名正文，如 TXT、MD、JSON | 复制完整原文，不复制渲染后的文本 | 主体进入详情；按类型预览、查看源码、元数据、删除；不可编辑 |
| 图片附件，如 PNG、JPG、WebP | 发起原文件下载 | 主体进入详情；图片预览、元数据、删除 |
| MD、JSON、PDF 等其他附件 | 发起原文件下载 | 主体进入详情；MD 渲染、可支持的预览或文件摘要、元数据、删除 |

复制或下载不自动返回。下载反馈只表示浏览器动作已发起，不代表文件已落盘；复制失败时在详情中提供可选择的原文。

#### 3.2.2 触摸与背景返回

| 操作 | 行为 |
|------|------|
| 短按 / 单击主体 | 进入详情，不直接复制或下载 |
| 右下角快捷区 | 执行块体复制或下载 |
| hover / focus / 长按 | 显示详情意图或进入详情；不触发快捷复制或下载 |
| 点击空白背景 | 无弹层时返回输入；不包括切换控件、反馈区或内容区域 |
| 弹层外点击 | 只关闭顶层弹层，不同时返回输入；再次点击背景才返回 |
| Esc | 优先关闭顶层弹层，无弹层时返回输入并恢复焦点 |
| 删除 | 二次确认后请求删除；成功返回输入，失败保留结果 |

复制、下载、删除、关闭及路由切换分别处理，避免一次触摸执行多个操作；主体短按或长按只打开详情及其中预览，快捷区直接复制或下载，键盘操作不依赖长按。

#### 3.2.3 异常边界

| 情况 | 处理 |
|------|------|
| 不存在或已过期 | 保留 key 并原位提示，不生成空白成功块 |
| 网络或服务异常 | 保留输入并允许重试，不伪装为内容不存在 |
| 正文读取失败 / 预览不可用 | 读取失败显示错误；已取得原始 Blob 但预览失败时保留下载，不再要求附件封装解析 |
| 有有效期元数据且本地推算已过期 | 提示状态，服务端响应仍是访问结果的依据；单条 GET 不含 expiresAt，缺失时不猜测 |

不自动复制、下载、打开 URL 或渲染不可信 HTML；缺失文件名、预览格式及编码规则见内容协议章节。

### 3.3 仪表盘页面


#### 3.3.1 瀑布流

响应式瀑布流展示当前认证范围内的 snip（不是本机发送历史）；顶部保留本地 key 搜索、刷新和存储摘要，不设统一设置区。

| 区域 | 内容与行为 |
|------|------------|
| 内容块 | 复用文本块或附件块，使用由容器和内容共同计算的稳定尺寸形成瀑布流；主体进入详情，仪表盘不设置传输快捷区 |
| 静止内容 | 常驻 PNG、MD、JSON 等具体类型标识；正文或图片内容仍隐藏，类型与状态遵循公共块约束 |
| 触摸详情 | 轻触或长按主体进入详情，按需请求内容并在其中展示摘要或适用预览；hover / focus 的详情意图提示不请求内容 |
| 元数据层 | 块外仅展示简短类型标识和状态；key、文件名、时间等在详情页展示 |
| 主体单击 | 打开统一详情容器，不直接下载或复制；复制、下载及删除位于详情内 |
| 动效 | 加载、筛选与过期切换采用轻量进出场及布局过渡；倒计时更新不触发布局重排 |
| 加载与失败 | 分页渐进展示，部分失败保留已有条目，未加载不能表现为无数据 |

稳定排序与确定性尺寸保证刷新、搜索后位置可预测，视觉阅读顺序与键盘顺序一致；瀑布流实现及虚拟化需求在组件章节确认。

#### 3.3.2 快照与离线 key 搜索

| 阶段 | 规则 |
|------|------|
| 首次进入 | 请求列表和统计，沿 cursor 顺序获取全部元数据分页，建立本地 key 索引 |
| 加载中搜索 | 仅匹配已获取条目，明确索引尚未完整；不宣称已搜索全部 snip |
| 搜索 | 在本地 key 上执行区分大小写的包含匹配，不发搜索请求；清空后恢复快照 |
| 手动刷新 | 重取列表全部分页与统计，保留搜索条件；失败保留旧快照并标明状态 |
| 后台与聚焦 | 不轮询，不因窗口聚焦、重新挂载或定时失效自动重取已有元数据快照 |
| 详情和预览 | 列表只返回元数据；主体单击进入详情时按 key 获取内容，详情内按 fileType 选择预览并缓存 |
| 本地写入 | 同一认证会话的发送、覆盖或删除成功后，按已知响应更新本地条目并失效对应内容缓存，不自动重取全部列表；统计保留快照并标明待刷新 |

离线搜索仅保证对当前已获取快照无网络匹配，不承诺首次离线访问、刷新后恢复或跨会话缓存；分页完成也不等同于后端事务快照。不预先下载正文和大附件，预览按需获取；未缓存内容断网时不可用。用户触发的读取与删除不属于元数据刷新。

#### 3.3.3 有效期与本地时间

| 项目 | 计算与显示规则 |
|------|----------------|
| 数据源 | 列表或创建响应的 expiresAt；明确为 null 才显示永久，尚未取得则显示未知；HTTP Expires 不是对象有效期 |
| 剩余时间 | max(0, expiresAt - 当前本地时间)，不依靠逐次减计数器累积 |
| 更新 | 使用共享时钟；远期按分钟等低频刷新展示，临近到期的可见条目可按秒更新，不请求接口 |
| 后台恢复 | 重新读取本地时间计算，不依赖后台定时器准时执行，不触发网络刷新 |
| 到期 | 以动效转为低强调过期状态，保留 key 和元数据，不立刻从瀑布流移除 |
| 已过期缓存 | 不继续作为有效内容提供复制或下载；需要时由用户明确重读确认 |
| 时钟偏差 | 本地剩余时间只是推算，不替代服务端有效性判断 |
| 存储统计 | 展示上次获取的服务端统计，不随本地到期伪造存储释放量或宣称 R2 已清理 |

手动刷新后以新列表对齐条目。多个条目过期不连续弹窗或强制滚动；reduced-motion 下直接更新状态。

#### 3.3.4 悬浮详情

单击内容块，以共享元素过渡展开悬浮详情，保留瀑布流滚动位置并使背景暂不可交互。关闭收回原位置；原条目已不在筛选结果时淡出，不强制改变搜索条件。

| 信息 / 操作 | 约束 |
|-------------|------|
| key、内容类型 | 索引 key、contentType，可复制 key；展示分类不是服务端 type 字段 |
| 创建时间、到期时间 | 来自索引 createdAt / expiresAt；显示绝对时间与本地剩余时间，明确 null 才标永久 |
| 对象大小 | 索引 size 为原始对象字节数；Page 不再使用 Base64 封装，无编码膨胀后的大小 |
| 实际读取大小 | 必要时显示 Blob.size；与索引值不一致时标明快照或传输差异，不虚构第二种编码大小 |
| 文件名、MIME | 索引 filename / contentType 与下载响应头；检测值仅作辅助并标明来源 |
| 来源 | 仅本会话创建响应中存在 source 时显示；列表与对象 GET 不公开 source，其他情况省略，不额外猜测 |
| 正文与文本附件 | 支持只读源码；MD 另提供渲染预览，复制始终使用原文，不允许编辑或修改类型 |
| 图片 | 对原始 Blob 安全解码并按比例预览，成功后可显示尺寸 |
| 其他文件 | 摘要及已支持的安全预览；无预览能力时不调用外部查看器 |
| 操作 | 复制文本、下载附件、删除确认；事件不透传到背景 |
| 关闭 | 遮罩、关闭图标或 Esc；恢复块体焦点与滚动位置 |

读取失败时保留已知元数据及错误状态。预览不执行 HTML、脚本或宏，不上传到第三方生成缩略图。


## 4. 原始对象协议与 API 契约


### 4.1 数据模型与分层

| 层级 | 数据 | 处理规则 |
|------|------|----------|
| 编辑与内容块 | 原始文本、File / Blob、raw 转换后的字节及本地选项 | fileType、contentRole、previewKind 属于前端状态，不写入后端 type |
| 上传请求 | 原始正文 + Content-Type + X-Snip-* Header | 不创建 JSON 外层 DTO，不使用 multipart，不统一编码为 Base64 |
| 创建响应 | key、contentType、filename、source、size、createdAt、expiresAt | JSON 校验后展示凭证并更新本地索引 |
| 列表响应 | items 中的 key、contentType、filename、size、createdAt、expiresAt，以及可选 cursor | 索引元数据，不含正文或 source |
| 单条读取 | 原始对象正文及 HTTP metadata | 使用 Blob / ArrayBuffer，按需解码为文本；不得统一 response.json() |
| 类型与能力 | fileType、contentRole、previewKind | 从 MIME、filename、下载语义及安全检测派生；具体文件类型可扩展，预览与右下角快捷行为分别决定 |

API 通过原始正文、HTTP metadata 和索引提供文件名、类型与大小；前端直接传输字节，不使用附件包装。

### 4.2 上传正文与 Header

#### 4.2.1 正文生成

| 输入 | 实际 HTTP body | Content-Type | 文件名 |
|------|----------------|--------------|--------|
| 当前文本块，含附件转文本结果 | 当前完整文本经 UTF-8 编码后的字节 | text/plain; charset=utf-8，不沿用附件 MIME，不因语法猜测改写 | 省略 |
| 直接选择、拖入或粘贴文件 | File / Blob 原始字节 | 可靠 MIME；未知用 application/octet-stream | 提供清理后的名称，缺失时生成回退名 |
| raw 原文手动转附件 | UTF-8 原文字节组成的 Blob | 与所选格式对应的 MIME，文本型注明 UTF-8 | 提供，如 settings.json |
| Base64 / Data URL / hex 转附件 | 用户批准后解码得到的原始字节 | 可靠候选 MIME，未知用 application/octet-stream | 提供指定名称或回退名 |

直接文件不重新编码、压缩或解压；文本不裁剪空白、不修改换行或格式化。Page 不开放空文本提交；空文件在部署验证前本地提示而不发送，不将此归因于后端。原始 JSON 同样只是对象正文，业务字段不作为请求控制参数。

#### 4.2.2 请求实现

请求使用当前准备好的字节或 Blob 作为 body，Token 只在运行时注入。随机 key 省略 X-Snip-Key；创建成功校验 201 JSON 并记录返回 key。非成功响应统一按 4.7 节处理。

#### 4.2.3 控制 Header

| Header | Page 规则 | 后端约束 |
|--------|-----------|----------|
| Authorization | Bearer Token | 必填；不存入对象 metadata |
| Content-Type | 显式设置，保留合法 charset 等参数 | 必填，合法 MIME |
| X-Snip-Source | 固定 page | 必填，1–256 字符 |
| X-Snip-Key | 自定义时发送；随机时省略，不发送空字符串 | 非空时 1–128 个 `[A-Za-z0-9_-]` 字符 |
| X-Snip-Filename | 附件发送 encodeURIComponent(安全文件名)，不重复编码 | 可选，后端解码后 1–1024 字符 |
| X-Snip-TTL | 发送十进制秒数字符串；永久时省略 | 可选正整数，缺省为永久 |
| X-Snip-Overwrite | 初次省略或 false，明确确认覆盖后才发送 true | true / false，默认 false |
| X-Snip-Meta-* | 首版不依赖其表达 UI 分类或解码协议 | 可存扩展 metadata，但公开 API 不回传，不能用于跨端还原展示 |

X-Snip-Source、文件名和扩展 metadata 共用后端 custom metadata 上限，key/value UTF-8 合计最多 8192 字节。Page 不在自定义头存 Token、正文、Base64 或大体积描述。

#### 4.2.4 HTTP metadata

后端支持保存并恢复 Content-Type、Content-Language、Content-Disposition、Content-Encoding、Cache-Control、Expires。Page 首版只主动提供 Content-Type 和 X-Snip-Filename；不开放通用 Header 编辑器，不让文件内容注入任意请求头。

不手动设置 Content-Length。上传 .gz / .zip 文件不等于设置 Content-Encoding: gzip；Page 原字节上传这些文件，避免浏览器透明内容解码改变下载结果。Expires 是 HTTP 缓存时间，绝不替代 X-Snip-TTL 或索引 expiresAt。

### 4.3 读取、分类与元数据可用性

#### 4.3.1 读取流程

```text
GET /snip/:key + Bearer Token
  -> 检查 HTTP 状态及响应头
  -> 非成功：按错误 JSON 契约处理
  -> 成功：读取原始 Blob / 字节
       -> 识别具体文件类型、正文或附件语义、预览能力
       -> 文本：安全解码后用于复制
       -> 附件：保留 Blob，用于下载及受限预览
```

对象正文即使是 JSON 也不能作为控制响应解析或按 error 字段判断失败；HTTP 200 的 HTML 也可能是合法对象，伪装模式见 4.7 节。

文件名依次取本次下载 Content-Disposition 的 filename*、filename、已有索引 filename，最后生成 `<key>.<ext>` 或 `<key>.bin`。按标准语法解析含引号参数，解码后清理路径和控制字符。

该回退名称只用于下载，不算服务端提供的 filename，不能据此把无名正文判为附件。附件语义仅来自实际文件名、Content-Disposition 或字节能力；读取时保留 Blob，严格解码失败仍可下载。

正文缓存保留 Blob 或字节，未知二进制不得先转字符串。Page 文本按 UTF-8 发送；读取按合法 charset（缺省遵循 MIME 约定）解码，无法支持或严格解码失败时降级为附件但保留原字节。下载使用显式 download，不将未知 HTML / SVG Blob 作为页面打开。

#### 4.3.2 可扩展文件类型与操作策略

文件类型由统一定义表驱动，不限三种展示枚举，也不为每种类型建独立页面。三页共享名称、图标、扩展名、MIME 映射和预览能力；发送页仅在文本转附件的编辑界面允许选择目标类型，所有已形成的附件块类型均只读。

| 维度 | 示例 | 职责 |
|------|------|------|
| fileType | txt、markdown、png、jpeg、webp、json、pdf、zip、custom、unknown 等 | 具体文件类型、块上标签与图标；可扩展 |
| contentRole | inline-text / attachment | 决定接收块右下角快捷区执行复制还是下载，不等同于文件类型 |
| previewKind | plain-text、markdown、raster-image、metadata-only | 决定预览组件；不支持预览仍可传输 |

| 首版类型组 | 下拉选项及类型标识 | MIME / 预览策略 |
|------------|--------------------|-----------------|
| 文本与标记 | TXT、MD、CSV、TSV、XML、YAML | 映射对应 MIME，MD 使用 text/markdown; charset=utf-8 并渲染，其余为源码预览 |
| 结构与源码 | JSON、HTML、CSS、JS、TS、SVG | 对应 MIME 与扩展名；JSON 等源码只读，HTML / SVG / 脚本不执行 |
| 位图 | PNG、JPG / JPEG、WebP、GIF | image/png、image/jpeg 等；通过检测和解码后预览 |
| 文档 | PDF、DOCX、XLSX、PPTX | 对应 MIME，首版元数据预览，不引入 Office 或 PDF 内嵌渲染 |
| 归档 | ZIP、GZIP、7Z、RAR | 对应 MIME，元数据预览，不自动解压 |
| 其他 | 自定义扩展名、无扩展名文件 | 可靠 MIME 或 application/octet-stream，安全默认无内容预览 |

读取或直接导入时综合 filename 扩展名、Content-Type 和可靠特征识别类型；JPG / JPEG 规范为 jpeg，但可保留安全扩展名。泛型 MIME 可由可靠文件名补充；无法可靠判定时统一使用 unknown，字段冲突标注差异并保守降级，不执行可疑内容。Blob 保留实际字节，但文件名与 MIME 不等于格式验证证明；不提供成块后手动修改类型的入口。

快捷区行为与类型分离：无名且可安全解码的 text/*、JSON / XML 正文作为 inline-text 复制；有 filename 或 attachment 语义的对象（包括 TXT、MD、JSON 文件）下载，位图及未知二进制始终下载。MD 附件可预览但仍按下载处理。

未取正文前按索引元数据显示类型，不为图标批量下载内容。接收和仪表盘类型只读；自定义类型使用标准文件名、MIME 和下载语义恢复，不依赖读不回的 X-Snip-Meta-*。

#### 4.3.3 各字段来源

| 信息 | 可用来源 | 缺失处理 |
|------|----------|----------|
| key | 请求 key、创建或列表索引 | 不从正文猜测 |
| contentType | 创建 / 列表字段、对象 GET 的 Content-Type | 未知则按通用附件处理 |
| filename | 创建 / 列表字段、Content-Disposition | 安全回退名称 |
| size | 创建 / 列表索引、GET 的 Content-Length、实际 Blob.size | 分别标明来源，不能盲目认为传输编码前后大小相等 |
| createdAt / expiresAt | 创建或列表 JSON | 对象 GET 不保证携带；缺失显示未知，不能用 Date / Expires 推断 |
| source | 仅创建 JSON 或本地发送上下文 | 其他客户端的 source 不在列表或对象 GET 公开，省略 |
| ETag | 对象 GET 响应头 | 作为响应标识，不擅自假定是内容 MD5 |
| 扩展 custom metadata | 当前公开 API 不回传 | 不承诺在详情展示 |

仪表盘已经有列表索引，可以显示创建及过期时间。接收页按 key 取回正文时优先使用同一认证会话的现有索引；用户明确展开元数据详情而索引缺失时，可分页查询 GET /snip 至找到该 key 或列表结束，不假定存在单条 metadata API。该显式动作不触发后台轮询，也不把局部查询误标为完整仪表盘快照；查询未找到或失败时，正文仍可使用，未知字段继续显示未知。

Page 上传不设置 Content-Encoding，索引对象大小与原始文件字节数应一致。其他客户端的 HTTP 内容编码对象可能被浏览器解码，Blob.size 与 Content-Length / 索引 size 因而可能不同；需明确显示来源，不能承诺恢复任意编码层的字节。

### 4.4 双向块转换、raw 解释与安全预览

块转换是发送草稿的本地内容准备，不改变 API 的原始正文协议。直接导入和文本转换得到的附件使用同一种模型；可选的转换前原文只用于附件转文本时恢复，不限制任一附件的转文本能力。

文本转附件时，先设置类型、名称和解释方式，再由 UTF-8 编码或显式解码生成 Blob；成块后不再修改类型。附件转文本时，若保留了本次转换前原文则直接恢复，包括 Base64 / hex 原文；否则按 UTF-8 将 Blob 解码为文本，允许二进制产生不可读字符，无效 UTF-8 字节使用替换字符。该操作是用户明确选择的内容转换，不要求结果为可读文本，也不承诺再次转附件能还原原二进制；不自动转换成 Base64 或十六进制文本。后续文本按 UTF-8 正文发送。

附件转文本成功后进入文本编辑，清除当前附件的类型、MIME 与文件名作用，保留 key / TTL；再次转附件以当前完整文本作为新的转换来源。取消、失败或超限保留操作前内容。原文恢复不等同于跨版本撤销历史，也不提供另一套附件类型或提交链路。

| 输入 | 自动行为 | 手动转换 |
|------|----------|----------|
| JSON / SVG / XML 原文 | 明确结构可推荐文件类型，批准前保留原文 | 默认按原文 UTF-8 保存，填写名称，不先做 Base64 解码 |
| 代码、Markdown、CSV 等原文 | 歧义较大，不自动转换 | 从文本块选择转附件，按 UTF-8 原文保存 |
| Base64 / Base64 Data URL | 只在候选解码后匹配可靠二进制特征时推荐 | 选择解码方式，得到原始字节后直接上传，不再次编码为 Base64 |
| 十六进制 | 合法解码并匹配特征后推荐 | 明确解码，允许未知类型以通用附件发送 |
| 二进制乱码 | 不承诺恢复原文件 | 可另存为 UTF-8 原文，不声称恢复了原始二进制 |

Base64 只接受标准字母表与合法填充，候选解码可移除 ASCII 空白；Data URL 首版只支持 Base64 分支。hex 允许连续十六进制或空白分隔的两位字节，以及单个整体 0x 前缀。不自动解析 Base64URL、百分号 Data URL、hexdump 地址列或字节脚本，不宽松修复、不静默换解码方式。

| 能力 | 首版类型 | 约束 |
|------|----------|------|
| 直接传输 | 任意扩展名或无扩展名文件 | 后端接受任意合法 MIME；Page 未知 MIME 使用 application/octet-stream |
| 手动原文保存预设 | TXT、MD、JSON、CSV、TSV、XML、YAML / YML、HTML、CSS、JS、TS、SVG | 名称和 MIME 不产生格式转换；自定义扩展名同样允许 |
| 自动文本推荐 | JSON 对象 / 数组、SVG、带 XML 声明的 XML | 使用结构解析；JSON 标量不推荐，XML 拒绝 DOCTYPE / ENTITY，不挂载到页面 |
| 自动二进制推荐 | PNG、JPEG、WebP、GIF、PDF、ZIP、GZIP、7Z、RAR | 推荐 file-type 浏览器入口；签名不是格式完整性或安全证明 |
| 位图预览 | PNG、JPEG、WebP、GIF | 安全解码且不超过 24 百万像素 |
| Markdown 预览 | MD / Markdown，含有名附件和无名 text/markdown 正文 | 使用 react-markdown 与 remark-gfm 渲染，支持标题、列表、表格等；可切换只读源码 |
| 其他文本附件预览 | UTF-8 文本预设 | 只读源码，最多预览 256 KiB，明确截取边界 |
| 其他内嵌预览 | 首版不支持 PDF / Office / 音视频 / 压缩包渲染 | 仍可下载，详情显示元数据 |

文本预览在完整 UTF-8 字符边界截取；自动预览及接收正文分类遇到无效字节时不使用替换字符冒充原文，而是回退附件操作。这与用户明确执行附件转文本不同，后者遵循上面的 UTF-8 转换规则。Markdown 沿用 256 KiB 预览上限并标明截取，限制嵌套深度和渲染节点数量；超出资源预算时回退源码。下载及复制使用当前内容的完整原字节或原文，不使用截断或渲染后的结果。

Markdown 不启用原始 HTML 解析，不内联 SVG、脚本或 iframe；禁用 javascript 等不安全 URL。远端图片不自动加载，链接不自动打开；显式跳转仅允许安全协议并提示外部目标，避免预览触发跟踪请求或泄露内容。任意 HTML / SVG 文件仍只显示源码。预览失败保留下载，不把渲染能力当作传输限制。

首版 Markdown 结构预算采用最多 10000 个节点、32 层嵌套；超限在同一详情内回退源码。位图在交给浏览器解码前读取受限头部并核对像素预算；头部不足或尺寸不可判定时保守回退下载。

用户确认文本转附件后保留本次转换前原文，之后通过同一个附件转文本操作恢复；直接导入附件没有该原文，仍可按上述规则转文本。拒绝、失败、超时都保留操作前内容。同一未变内容拒绝后不重复自动弹窗，手动入口保留。可终止的检测任务上限为 2 秒，仅保留当前输入快照；执行环境及 Worker 分工在后续章节确定。

检测与显式转换均在独立 Worker 执行，采用同样的 2 秒终止上限。转换参数界面离开路由时视为取消尚未确认的本地转换，恢复进入前的编辑 / ready 状态及原内容；这不取消已发出的上传请求。XML 校验使用纯 JavaScript 结构校验器，避免假定 Worker 存在 DOMParser。

### 4.5 大小、名称及有效期

| 项目 | 新 API 约束 / Page 策略 |
|------|-----------------------|
| 单对象上限 | README 默认 10 MiB，前端使用可配置的 10 * 1024 * 1024 字节基线；部署值仍需对齐 SNIPFLOW_MAX_SNIP_SIZE |
| 前端大小配置 | VITE_MAX_OBJECT_BYTES，默认 10485760；构建时校验为正安全整数，与后端部署上限同步配置 |
| 文本计量 | TextEncoder 的 UTF-8 字节数 |
| 文件计量 | 实际 File / Blob.size，不再叠加 JSON 与 Base64 膨胀 |
| raw 转换计量 | 以最终解码或 UTF-8 生成的字节数判断可否上传，不按 Base64 字符长度限制对象大小 |
| raw 检测输入 | 暂定原文 UTF-8 最大 24 MiB；解码前估算输出，检测超限不截断或删除输入 |
| 文件名 | 后端解码后 1–1024 字符；Page 可采用更保守的 255 UTF-8 字节安全文件名，保留字符边界和扩展名 |
| key | 自定义 1–128 个允许字符；随机时省略 Header；读取与删除不允许空 key |
| 有效期 | 前端预设 1 小时、24 小时、7 天、永久；默认发送 X-Snip-TTL: 86400，永久省略此头 |
| 存储展示 | storageLimit 默认 100 MiB，仅为展示值，后端不据此强制全局限额；Page 不因剩余展示值不足直接禁止上传 |

有效期以创建 / 列表 expiresAt 为准；Expires、HTTP Date、ETag 均不用于推导对象 TTL。前端本地倒计时不产生网络请求或声称 R2 已完成清理。

### 4.6 路由与响应契约

以下为 Worker 的资源路径。受保护请求使用 Bearer Token，开发代理与生产同源分流必须转发完整字节和控制 Header，不将对象请求重写为 JSON。

| 方法 / 路径 | 请求 | 成功响应 |
|-------------|------|----------|
| GET /health | 无认证 | 200，健康 JSON |
| GET /health/auth | Bearer Token | 200，校验 ok 与 authed 为 true |
| POST /snip | 原始 body + 必需 Header | 201，索引 JSON，包含 source |
| GET /snip | 可选不透明 cursor，URL 编码 | 200，items 索引与可选 cursor，每页最多 100 条 |
| GET /snip/:key | 合法 key 路径段 | 200，原始正文及 HTTP metadata，不是 ReadSnipResponse JSON |
| DELETE /snip/:key | 合法 key 路径段 | 204，无正文 |
| GET /stats | 无查询 | 200，count、totalSize、storageLimit |

```ts
type SnipIndex = {
  key: string
  contentType: string
  filename?: string | null
  size: number
  createdAt: string
  expiresAt: string | null
}

type CreateSnipResponse = SnipIndex & { source: string }
type ListSnipsResponse = { items: SnipIndex[]; cursor?: string }
```

以上为前端兼容解析模型：README 未展示无文件名响应的完整例子，因此容忍 filename 缺省或 null，不把它当作新后端必须同时支持两种形态的承诺。contentType 校验合法 MIME，size 为非负安全整数，时间字段要求可解析且含时区，expiresAt 明确 null 才代表永久。

不向读取正文套用 Zod 对象 Schema；只校验控制 JSON、索引及已提取的响应头。列表不包含 source 或 custom metadata，额外未知字段忽略但不依赖。

### 4.7 缓存、未知结果与错误处理

| 情况 | 处理 |
|------|------|
| 400 INVALID_INPUT | 按 error.issues 映射 Header / 字段问题，保留草稿 |
| 401 UNAUTHORIZED | 清除失效 Token 与当前页面数据，返回输入；不进行草稿暂存或恢复 |
| 404 NOT_FOUND | 读取显示不存在；删除时同步本地“目标不存在”，不伪造 204 |
| 409 KEY_CONFLICT | 修改 key 或明确确认 X-Snip-Overwrite: true |
| 413 PAYLOAD_TOO_LARGE | 保留原字节，按实际对象大小提示，不自动压缩或分片 |
| 405 / 415 | 检查请求方法和必需 Content-Type，保留原始正文契约 |
| 5xx / 网络错误 / 超时 | 保留草稿及 requestId，写请求结果可能未知，不自动重试 POST / DELETE |
| 204 | 不解析 JSON |
| 成功的 JSON / HTML 对象 | 作为用户对象，不因正文长得像 error 或 Hello World 就判失败 |
| 控制端点返回 200 HTML | 控制响应不满足预期 JSON Schema，提示认证或协议异常 |

**伪装模式限制：** 原始对象可以合法等于 Hello World HTML，甚至合法等于错误 JSON。对象 GET 的 200 伪装响应与真实对象可能无法仅凭公开响应可靠区分。推荐正式给 Page 使用的 API 配置 SNIPFLOW_DISGUISE=false；若开启，客户端只能提示存在歧义，不能靠正文猜测或认证预检保证每次对象读取成功。此为当前协议限制，不虚构额外标识头。

`error.issues` 的内部结构不由 README 保证。客户端对可识别的 path / field 映射为 key、有效期、文件名或格式提示；未知项保持通用拒绝提示，不展示任意服务端输入值，也不凭空要求后端增加字段。

随机 key 上传响应丢失时，客户端可能不知道服务器生成的 key，不承诺查回或幂等重发。用户取消请求不等于撤销服务器写入。

列表按 cursor 顺序加载、按 key 去重；重复 cursor、失败或取消时索引标记不完整。同一会话写入成功可根据响应更新已知索引并失效对应 Blob 缓存，不为此自动刷新全列表；窗口聚焦和过期倒计时不轮询。手动刷新才重新建立远端索引快照，统计保留其快照属性。

浏览器请求应避免 HTTP 缓存替代实际鉴权，对象读取采用 cache: no-store，Page 仅按已确认规则维护当前认证会话的内存缓存。对方上传的 Cache-Control / Expires 不自动改变前端保密和刷新策略；最终配置在鉴权及部署章节验证。

已缓存正文的显式重读返回 404 时，将旧正文标为失效，不能继续复制 / 下载；可保留摘要和错误提示供重试，只有之后的明确读取成功才恢复可用。此失效不触发自动重读。

新文档不保留上一文档的创建覆盖层，远端列表可能暂时落后于按 key 的读取。此时允许用户明确刷新索引，不伪造条目或增加后台轮询；发布测试需按该一致性边界等待，而非假定创建后每个新列表立即包含对象。

### 4.8 验收要求

| 场景 | 验收要求 |
|------|----------|
| 正文与类型 | 未执行内容转换时文本 UTF-8 及二进制原样往返；类型仅在文本转附件前设置，直接导入未知类型仍可传输，附件成块后类型只读，不伪装转码 |
| 转换与预览 | 两种块转换都只在本地准备阶段发生；有来源原文的附件转文本恢复原文，无来源的附件包含二进制也可转文本；预览不冒充该转换，MD 可切换渲染与源码，不执行 HTML 或外部资源 |
| 文件名与元数据 | 非 ASCII 文件名正确编码、回取和清理；索引 JSON、Blob、未知字段及 Content-Encoding 大小差异按来源处理 |
| key、TTL、容量 | 随机 key 和永久 TTL 省略对应 Header；覆盖需确认；按对象字节验证单对象限额与展示用 storageLimit |
| 错误与伪装 | 控制 JSON 与合法 JSON / HTML 对象分流；覆盖 4xx、5xx、网络错误及伪装模式不可完全判定的限制 |
| 仪表盘 | 本地 key 搜索、倒计时、元数据缺失和无周期轮询策略正确 |

## 5. 前端结构与状态管理


### 5.1 模块划分

```text
page/
  src/
    app/                  应用入口、Provider、路由和页面切换
    pages/                Auth、Send、Receive、Dashboard 页面组合
    features/
      auth/               认证会话与路由守卫接口
      send/               草稿、双向块转换、转换前参数和提交协调
      receive/            key 输入、对象读取和主操作
      dashboard/          索引快照、本地搜索与详情协调
    components/
      content-block/      三页共享块体和局部操作入口
      preview/            图像、Markdown、源码及元数据预览
      ui/                 Base UI 控件封装与公共交互
    domain/               内容模型、类型定义表、校验及纯函数
    api/                  HTTP 客户端、Header 映射与响应校验
    queries/              查询键、查询和 mutation 定义
    stores/               会话内 UI 状态与草稿 store
    workers/              raw 检测及受限内容处理任务
    hooks/                Object URL、共享时钟等生命周期适配
    styles/               设计令牌与全局样式
    test/                 测试初始化、MSW handler 与 fixture
  e2e/                    浏览器流程测试
```

模块目录为职责规划，文件在功能实现时按需创建，不预生成空占位模块。单元及组件测试与被测文件相邻，公共 fixture 和端到端测试分别集中管理。

| 层级 | 允许职责 | 禁止职责 |
|------|----------|----------|
| 页面 | 布局组合、关联路由和 feature | 直接解析 Header、Base64 或复制业务状态机 |
| feature | 协调交互、状态及查询 | 重写公共文件类型规则或直接修改其他 feature 私有状态 |
| 公共组件 | 按 props 展示并派发语义事件 | 获取 Token、调用后端、推测当前页面执行删除或发送 |
| domain | 类型、纯转换、输入及协议边界规则 | 依赖 React、路由、store 或网络客户端 |
| api | 请求、取消信号、原始字节与控制 JSON 分流 | 导航、弹窗、全局草稿或自动写请求重试 |
| queries | 服务端数据缓存、请求键和显式更新 | 保存输入焦点、选项面板开关或编辑原文 |
| stores | 当前会话的草稿和共享界面状态 | 复制完整服务端索引或在持久化插件中保存 Token、Blob |

页面通过 feature 使用查询和 store；api 与 domain 不反向依赖界面。所有 ContentBlock 的主体、快捷区、详情层事件均由 feature 提供语义回调，公共组件不在内部猜测“单击即复制”或“单击即下载”。跨 feature 的同步由查询缓存更新或 app 会话事件协调，避免互相导入页面组件。

### 5.2 状态所有权

| 状态 | 唯一所有者 | 生命周期 |
|------|------------|----------|
| 当前页面 | TanStack Router | URL 与浏览器历史；不另存一份 currentPage |
| Token 与认证阶段 | auth 模块 + localStorage | 模块管理当前值，localStorage 保存凭据和续期时间；规则见第 6 章 |
| 发送草稿及选项 | Zustand send store | 跨发送 / 接收路由保留，仅在当前认证会话内有效 |
| 接收 key、当前结果引用 | Zustand receive store | 返回输入保留 key；正文只引用查询缓存 |
| 搜索词、排序、详情 key、滚动锚点 | Zustand dashboard store | 同一会话内保留，不复制索引正文 |
| 服务端索引、统计、对象 Blob | TanStack Query | 按认证会话隔离，显式刷新或已知 mutation 更新 |
| Popover 打开、局部焦点、手势进度 | 组件状态 / ref | 组件卸载释放，不加入全局 store |
| 类型标签、复制 / 下载能力 | domain 派生值 | 从当前草稿或元数据计算，不重复写入独立状态 |
| Object URL 与解码器实例 | 预览组件或生命周期 Hook | 需要时创建，失效与卸载时释放 |
| 过期倒计时 | 页面共享时钟派生 | 仅更新展示，不逐块创建网络轮询 |

草稿保存当前文本或 File / Blob 引用，不同时持有无用的 Base64、ArrayBuffer 与解码副本。文本转附件时额外保留本次转换前原文供反向转换恢复；该字段对直接导入附件为空，不构成附件子类型，所有附件使用相同转文本操作。key / TTL 属于草稿；附件名称、MIME 与类型属于当前附件，不能残留为文本发送 Header。

### 5.3 路由与公共组件

| 单元 | 职责 |
|------|------|
| AuthGate | 有效缓存直接放行，否则显示 Token 输入；手动验证成功后进入站内目标，不按路由切换重复握手 |
| TransferShell | 承载发送 / 接收共享背景、边缘方向控件及过渡；不显示仪表盘入口 |
| DashboardPage | 独立路由页面，复用公共块和预览，但不进入发送 / 接收手势序列 |
| ContentBlock | 接收类型、状态、主操作语义与详情页选项配置，呈现统一块体 |
| BlockActions | 在详情内按上下文提供发送选项、适用的双向转换、复制、下载或删除；不自行决定权限 |
| FileTypeSelect | 仅在文本转附件的编辑界面读取类型定义表、设置目标类型；不在已形成的附件块上出现，不直接更改正文或上传 |
| PreviewSurface | 在统一详情内选择图像、Markdown、源码、摘要 renderer，管理加载与错误回退，不另开预览浮层 |
| DetailDialog | 主体点击或长按打开的统一容器，组合元数据、适用预览及可展开选项，约束焦点、遮罩及关闭事件 |

ContentBlock 使用明确的上下文契约：发送块仅提供发送和本地编辑类事件，接收块提供复制 / 下载，仪表盘块提供打开详情。避免通过一个含大量互斥布尔值的通用组件暴露所有操作。

类型定义表集中维护 id、标签、扩展名、MIME、预览与转换前目标约束；状态模型保存转换前声明和检测证据，派生类型与预览权限。文本转附件确认时一次性提交 Blob、名称、MIME、类型和草稿版本，附件成块后不允许用户修改类型。附件转文本恢复已有原文或解码 Blob，统一回到文本编辑；两种转换都保留 key / TTL，不在任何页面另建附件子类型。

### 5.4 发送状态流

使用 TypeScript 可辨识联合与 Zustand action 表达状态，不引入额外状态机库。每个异步操作绑定 sessionId、draftId、revision 和 operationId。

```text
文本 editing / 文件导入 -> preparing -> ready（文本或附件）
ready -> sending -> sent / failed / conflict / uncertain

文本 -> converting（设置类型、名称、解释方式）-> ready（附件）
附件 -> converting（恢复已有原文或解码 Blob）-> editing（文本）
转换取消 / 失败 -> 操作前状态；文本重新编辑 -> editing
```

| 状态 | 允许操作与约束 |
|------|----------------|
| editing | 文本编辑、文件导入、raw 候选检测；内容变更递增 revision |
| preparing / converting | 文件校验或双向块转换；类型设置只在文本转附件确认前，取消或失败回到操作前状态，保留完整内容 |
| ready | 详情、预览、key / TTL 与最终发送；文本可重新编辑或转附件，所有附件可转文本、移除或替换，附件类型只读 |
| sending | 固定提交快照，禁止修改当前块和重复发送；切换路由不取消已发出的写请求 |
| sent | 显示服务器 key，提供只读详情、复制凭证与新建；无再次发送或块转换动作 |
| failed | 明确校验或服务端拒绝，保留内容，用户修改或明确重试 |
| conflict | 已取得 409，修改 key 或二次确认覆盖；仅该确认产生覆盖请求 |
| uncertain | 响应丢失、取消或不可确认的服务端错误；明确提示可能已保存，不自动重试 |

双向块转换只针对本地可修改草稿，接收和仪表盘保持只读。failed 在用户明确返回修改后恢复适用的草稿操作；conflict 先修改 key 或确认覆盖，若放弃该次提交返回修改则撤销覆盖意图；uncertain 须先明确放弃等待本次结果、接受可能已保存的提示后才能回到修改，转换不隐含重发。sending / sent 均禁止转换。

提交快照由请求协调层冻结，mutation 请求阶段由 TanStack Query 提供；store 只保存块阶段、操作身份及必要结果引用，不维护另一套独立请求计时器或 pending 真值。所有阶段变更通过协调 action 进行，回调必须校验身份再提交结果。

网络结果到达时先按服务端真实结果更新同一会话的缓存；若用户已经切换草稿，则不得替换当前块，但可把结果归入对应操作记录。认证会话改变后，迟到结果全部丢弃，不写入新会话缓存。

### 5.5 读取与详情状态流

| 流程 | 状态及约束 |
|------|------------|
| 接收 | input -> loading -> result / error；提交新 key 使旧请求失去页面写入资格 |
| 返回输入 | 清除结果展示引用，保留 key；正文可继续由受限会话缓存复用 |
| 预览 | idle -> loading -> ready / unsupported / error；预览错误不改写原对象 |
| 删除 | idle -> confirming -> deleting -> done / error；确认前不请求，进行中不重复触发 |
| 元数据补取 | 仅在明确展开详情且字段缺失时进行；正文可先独立显示，不伪造缺失字段 |
| 仪表盘详情 | 保留选中的 key 与来源块锚点；关闭详情取消其界面订阅，恢复列表位置 |

读请求可按需求取消，但共享读取仍被其他组件使用时不能由一个面板直接中止底层请求。query 层协调取消与去重；复制、下载由用户事件直接发起，不放进 Effect，以免渲染或重挂载重复触发。

### 5.6 查询键与快照一致性

查询键中的 sessionId 为本地随机会话标识，不是 Token 或其散列。Token 通过 auth 模块注入 api，不出现在日志、URL、查询键或开发工具数据中。

| 数据 | 查询身份示意 | 更新规则 |
|------|--------------|----------|
| 全量元数据快照 | [sessionId, snips, snapshot] | 首次加载和用户刷新；含完整度、游标、快照时间 |
| 统计 | [sessionId, stats] | 与仪表盘明确刷新同步，不随倒计时自行改写 |
| 对象正文 | [sessionId, snip, key, body] | 用户取回或打开预览时按需获取，同 key 并发复用 |
| 局部元数据补取 | [sessionId, snip, key, metadata] | 不冒充完整列表；成功可补充已知条目 |

不使用默认的聚焦、重连、重新挂载或定时刷新机制更新索引快照。stale、回收和错误重试分别配置；本章默认不自动网络重试，用户通过明确操作重试。正文缓存命中后可复用；已知过期、覆盖、删除或用户明确重取时按规则失效。

分页刷新通过 generation 隔离：首次加载可渐进显示；已有完整快照时刷新构建新候选，完成后统一替换。失败保留旧快照并标记刷新失败，不混成伪完整列表。手动重复刷新取消前一轮，重复 cursor 或分页错误停止并标记不完整。

分页期间发生本地创建、覆盖或删除时，记录当前会话的变更序号与已知条目覆盖层，在新快照发布时重放这些变更，避免较早开始的列表响应复活已删除条目或覆盖新创建结果。后续一次从写操作完成后启动的显式刷新可重新对齐远端；不声称 KV 列表具备事务一致性。

成功创建 / 覆盖更新已知索引并失效同 key 正文；成功删除清除已知条目、局部 metadata 与正文。相关统计保留快照并标为待刷新，不假设覆盖使数量加一。所有更新限定当前认证会话。

### 5.7 原始内容与预览资源

| 资源 | 所有权及释放条件 |
|------|------------------|
| Blob 缓存 | 仅 Query 保存服务端原始正文，不在 Zustand 复制；预览按引用使用 |
| 草稿 Blob | send store 管理，提交成功后新建、移除或退出认证时释放引用 |
| Object URL | 渲染器或下载 Hook 创建；预览关闭、字节引用变化后释放；下载 URL 延后释放，避免取消刚发起的下载 |
| raw 检测任务 | 请求包含 sessionId / draftId / revision；替换输入或退出会话终止，超过 2 秒终止执行 |
| MIME / raw 处理 | 可放入 Worker 的计算与主线程 DOM 解析分开；不得假设 Worker 拥有 DOMParser 或完整 DOM |
| 图像解码资源 | 预览失效后释放，超出像素或格式限制不继续解码 |
| Markdown 预览 | 按确认的长度、节点和嵌套预算执行；关闭后取消后续工作，不留隐式外链请求 |

正文缓存暂定累计上限 50 MiB、最多 20 个未活动对象，按最近使用情况驱逐未订阅项；正在使用的对象不被强制释放。索引不受这个条目数量限制。若活动资源已超过预算，新的预览不额外保留缓存，并关闭非必要预览；不能仅配置时间回收而忽略大文件的实际字节占用。

元数据快照在当前认证会话内保留，正文通过字节 / 条目预算回收，不用默认的短时 gcTime 让路由重新挂载隐式重取快照。超过 24 MiB 的对象不自动做完整文本解码，保留原 Blob 下载；已知覆盖或本地到期的旧正文暂停复制 / 下载，用户明确重读成功后以最新服务端结果为准。

完整手动刷新同步已知对象的局部元数据并使相应正文缓存失效，但不主动下载正文；用户再次提交 key 或打开详情时才按需重读。完整快照中没有某 key 不等于它当前不存在，用户明确打开新取回对象的详情时仍可补取元数据。

该预算不代表浏览器总内存上限；草稿、UTF-8 解码字符串、图像像素和解析树另需遵守各自限制。内存管理只管理缓存引用，不创建第二份正文缓存。具体驱逐实现及浏览器适配在开发时通过压力测试验证。

### 5.8 交互与动画协调

| 行为 | 约束 |
|------|------|
| 路由手势 | 只有完成阈值判断后提交导航；中途取消只复位视觉状态 |
| 动画与业务 | 动画订阅业务状态，不由动画结束事件决定上传成功、认证结果或删除是否完成 |
| 输入互斥 | 点击、长按、拖动、背景返回通过明确手势阶段协调；长按结束抑制同次 click |
| 弹层 | 一次只由顶层可交互面板接收关闭事件；确认层关闭不触发底层复制、下载或返回 |
| 异步预览 | 先校验当前对象身份再显示；关闭后的结果不得重新打开弹层 |
| 过期显示 | 单个页面共享时钟，按可见条目所需精度更新；隐藏后暂停高频更新，恢复时按当前时间重算 |
| 无障碍 | 动画关闭时操作语义和焦点恢复不变；删除后原块不存在时焦点转到下一可用条目或列表标题 |

### 5.9 验收要求

| 场景 | 验收要求 |
|------|----------|
| 路由切换 | 草稿保留，不建立第二份页面状态，不自动刷新仪表盘快照 |
| 输入变化 | 旧 raw 检测或预览结果不能替换当前内容 |
| 请求竞争 | key 快速切换、重复刷新、跨页响应各自隔离，取消不产生假成功 |
| 认证变化 | 退出或 Token 变化时清理当前缓存；已失效操作的回调不重新填回页面 |
| 块转换 | 文本转附件确认时标签、文件名、MIME、Blob 与预览同步；附件转文本恢复原文或解码 Blob，成块后无类型修改入口 |
| 分页与写入 | 刷新期间创建或删除不被较早结果覆盖，失败不标完整 |
| 内存释放 | 关闭预览、切换内容、清除会话后释放 URL 和任务；缓存淘汰不破坏活动查看 |
| 交互互斥 | 长按不附带下载，关闭弹层不返回输入，动画不重复触发业务动作 |

## 6. Token 缓存与认证


### 6.1 缓存与部署配置

Token 使用浏览器 localStorage 保存。同源标签页可以复用有效缓存，刷新页面不要求重复输入；每次发送、接收、列表、统计和删除请求仍携带 Authorization: Bearer <token>。

| 配置 / 数据 | 定义 |
|-------------|------|
| 缓存键 | snipflow.auth |
| 缓存值 | JSON 对象，包含 token 与 lastActiveAt |
| lastActiveAt | 最近一次功能页续期时间，毫秒时间戳 |
| 部署配置 | VITE_AUTH_IDLE_TTL_SECONDS，默认 1800；0 表示永不过期；配置本身不包含 Token |
| 配置校验 | 仅允许非负安全整数，部署构建时校验，错误配置不能意外变成永久 |
| 过期条件 | 新进入应用时，配置不为 0 且当前时间与 lastActiveAt 的差值大于等于配置时长 |
| 缓存异常 | 缺字段、JSON 损坏或无效时间戳视为未登录，清理后重新输入 |

这是前端缓存闲置期限，不是后端 Token 的有效期，也不影响对象的 X-Snip-TTL。后端仍只检查请求中的 Token 是否正确；即使配置永久，后端拒绝该 Token 时仍需重新输入。

### 6.2 停留续期

**持续停留在功能页面时不过期，不要求鼠标或键盘操作。** 发送、接收和仪表盘都属于功能页面；停留认证页不续期。

| 场景 | 行为 |
|------|------|
| 进入功能页 | 更新 lastActiveAt，开始本地续期 |
| 持续停留 | 每 30 秒更新一次本地时间戳，不调用 /health/auth 或其他网络接口 |
| 发送 / 接收切换 | 更新续期时间，保持认证，不重复校验 Token |
| 切到其他标签页 | 本应用仍打开时继续视为停留，不因不可见主动登出 |
| 页面暂停后恢复 | 若是同一仍已认证的文档且缓存 Token 未被清除或更换，先续期，不因后台定时器暂停而退出 |
| 离开应用或关闭页面 | 尽力在 pagehide 写入离开时间；此后按闲置期限计算 |
| 重新打开 / 刷新 | 从缓存判断期限，有效直接进入目标页面，已过期则重新输入 |
| 永不过期 | 不进行本地时间过期判断，直到用户退出、清理浏览器存储或后端拒绝 Token |

过期检查不在已认证且停留中的页面按定时器强制执行。续期前确认缓存仍是当前 Token，不能把其他标签页已清除的凭据重新写回。多个标签页共用最近续期时间，任一正常运行的功能页都可延长缓存使用期。

浏览器强制结束进程、丢弃标签页或冻结全部相关文档时，页面代码无法保证持续执行；再次作为新文档加载时按最后保存时间判断。这是浏览器执行限制，不加入后台服务或复杂标签页存活协议。正常关闭事件未执行时，以上一次本地续期时间为准。

### 6.3 进入、验证与退出

```text
打开页面
  -> 有有效缓存：直接进入目标页面
  -> 无缓存 / 已过期：输入 Token
       -> GET /health/auth 验证
       -> 成功：缓存 Token，进入目标页面
       -> 失败：保留输入，显示错误
```

| 操作 / 状态 | 规则 |
|-------------|------|
| 手动验证 | 只在用户提交 Token 时调用认证接口；200 且 ok / authed 为 true 才保存 |
| 有效缓存 | 直接使用，不在刷新或页面切换时额外发握手；真实 API 请求仍由后端鉴权 |
| 目标页面 | 保留 /send、/receive 或 /dashboard；无指定目标进入 /send |
| 请求注入 | api 模块统一读取当前 Token 并设置 Authorization，页面组件不重复拼接 |
| 401 | 清除本地凭据、停止续期、清空当前界面和服务端缓存，返回认证页 |
| 普通网络错误 | 不删除 Token、不强制登出，显示错误并允许用户重试 |
| 主动退出 | 删除缓存、停止续期、清理当前页面状态，进入 /auth；不删除远端对象 |
| 其他标签页退出或换 Token | 通过 storage 事件停止使用当前凭据并重置页面；缓存未变时仅同步续期，不清空内容 |
| 无可用 localStorage | 明确提示无法保存，临时仅内存使用；刷新需重输，不伪装为已缓存 |

简单保留认证校验中的 loading 标志，防止重复提交；退出或换 Token 后忽略迟到响应即可，不增加独立恢复流程。缓存恢复不承诺离线权限验证；当前是个人共享 Token 模型，断网时已有本地内容仍可按原规则查看。

功能页因退出、401 或跨标签页清除凭据而回到认证时，也保留当前功能路由作为本次重新验证的站内目标；目标仅存内存，不连带保留草稿或正文，认证页刷新后没有目标则回到发送页。

### 6.4 页面数据与基本边界

| 项目 | 规则 |
|------|------|
| 草稿 | 仅指当前未发送的页面内容，切换发送 / 接收可保留；不持久化、不建立待恢复槽或跨认证草稿 |
| 刷新 / 退出 / 401 | 清除当前内存草稿和预览，重新进入不提供恢复弹窗 |
| 普通上传失败 | 同一页面保留未发送内容便于重试，不属于认证恢复机制 |
| 退出确认 | 有未提交内容或正在发送时，使用一次简单确认说明可能丢失内容；401 不等待确认 |
| 缓存清理 | 重置相关 Zustand 状态和 Query 数据，释放预览 URL，停止任务；无需复杂安全擦除 |
| 请求取消 | 尽力取消本地请求，不代表后端已回滚；不自动重发未知结果的上传 |
| 凭据暴露 | localStorage 中的 Token 可被同源脚本读取，不宣称加密或严格隔离；这是个人项目接受的取舍 |
| 基础约束 | Token 不硬编码进仓库、不写入 URL 或日志；预览不执行脚本；继续保留后端 Bearer 校验 |

页面功能、预览安全和网络错误处理继续遵循前文，不引入角色权限、Token 轮换客户端、跨标签页草稿同步或其他企业级机制。

### 6.5 验收要求

| 场景 | 预期 |
|------|------|
| 首次输入 | 正确 Token 验证后保存，错误 Token 不覆盖有效缓存 |
| 期限内重开 | 无需重复输入，继续发送和接收 |
| 离开超过 30 分钟 | 默认配置下重新输入；可使用更短测试配置验证边界 |
| 持续停留 | 超过 30 分钟仍可使用，续期不产生网络请求 |
| 后台恢复 | 同一文档恢复时续期，缓存已被退出清除时不得复活 |
| 永不过期配置 | VITE_AUTH_IDLE_TTL_SECONDS=0 时不按时间清除 |
| 后端拒绝 / 主动退出 | 清缓存并回认证页，不执行远端删除或草稿恢复 |
| 刷新 | 只恢复有效 Token，不恢复输入内容、文件或仪表盘正文缓存 |

### 6.6 同源发布边界

首版部署入口使用 Cloudflare 静态 ASSETS 与 API Service Binding；API 路径优先转发，前端已知路由才回退 index.html，缺失脚本和 API 404 不回退成 HTML。API 不使用 HTTP 缓存，入口 HTML 不缓存，带哈希静态资源可长期缓存；静态页面 CSP 只允许同源脚本、请求和 Worker，图片允许 Blob，动态布局保留内联样式。配置及回退见 deploy/README.md。

API Bearer Token 只用于业务请求，不是云账户部署授权。预发布须有独立 API、存储和 Token；仅部署第二个 Page 不算隔离环境。分别记录开发代理、本地产物同源、独立预发布和线上域名验收：本地适配器不证明线上 Service Binding 或域名路由已经生效，缺少账户 / 服务名 / 独立凭据时保留发布门禁未完成，不用模拟测试冒充。

## 7. 视觉规范与动效

> 状态：已确定。本章确定首版视觉基线及交互动画，不改变已确认功能，不新增动效框架。具体效果须通过后续浏览器原型验证。

### 7.1 整体方向

界面以白色与近黑文字建立清晰层级，浅蓝、浅粉表达页面氛围，浅绿表达完成状态。高级感来自克制而精确的层次、顺滑且有目的的动效、稳定的布局和对细节的及时回应，而不是堆叠装饰。每一次点击、触摸、键盘触发和状态变化都应有即时、可理解且不过度的反馈；过渡应连续、自然、可被打断，并在操作完成前后保持清晰的因果关系。内容区保持直接、轻量，不用外层大卡片包裹主体验，不出现功能介绍、教程或营销版式。

| 层级 | 视觉定义 |
|------|----------|
| 页面背景 | 满幅白底与低饱和宽色带；色带延伸出视口，不形成独立光球、粒子或漂浮装饰 |
| 主输入 | 白色或近白表面、细边框、明确焦点；不附加夸张玻璃效果 |
| 内容块 | 浅色实心表面、类型图标和简短类型标识，低高度阴影；不常驻正文或缩略图 |
| 局部操作 | 贴近对象的图标入口，hover / focus 提高对比；不围绕内容堆放文字按钮 |
| 预览与详情 | 同一模态层内使用独立的白色详情块与预览块，两块之间留出间隙，内部不再套卡片；周围背景轻度减弱 |
| 仪表盘 | 独立的浅色工作界面，搜索与统计紧凑，瀑布流直接置于背景上 |
| 交互反馈 | 按下、聚焦、加载、成功、失败和禁用均有清晰的局部反馈；反馈不改变无关布局，不依赖颜色或动画单独传达含义 |
| 过渡节奏 | 进入、退出、展开、收起和页面切换保持统一节奏；允许用户连续操作，新的操作可安全接管尚未结束的过渡 |
| 自适应表现 | 组件以容器、内容和输入方式为依据动态调整密度与可操作区域，在窄屏、宽屏、触屏和键盘环境下都保持舒适可达 |

背景使用 CSS 色带及必要的局部渐变，不引入 Three.js、Canvas 背景或持续 GPU 渲染。图标使用 Lucide，图片预览使用真实文件；不使用虚构缩略图填充未知文件。

### 7.2 色彩令牌

| 令牌 | 初始色值 | 用途 |
|------|----------|------|
| canvas | #FAFBFC | 页面基础背景 |
| surface | #FFFFFF | 输入、选项面板、预览与详情表面 |
| ink | #202327 | 主文字及主操作图标 |
| muted | #626970 | 次要元数据 |
| border | #DDE2E7 | 装饰性分隔与表面边界，不作为唯一交互识别依据 |
| blue-soft / blue-ink | #EAF3FC / #2B5D80 | 仅用于发送页背景与模式提示 |
| pink-soft / pink-ink | #FCE9F0 / #A83F67 | 仅用于接收页背景与模式提示 |
| green-soft / green-ink | #EAF5EE / #2F6B4B | 完成反馈、成功图标 |
| neutral-soft | #F0F2F4 | 中性辅助表面 |
| danger-soft / danger-ink | #FBEDEE / #A33746 | 明确错误与删除操作 |
| focus | #356A94 | 可见焦点环 |

色值作为集中管理的 CSS 变量，组件不各自挑选相近颜色。普通文字对比度目标至少 4.5:1，必要控件边界和焦点目标至少 3:1；上述初始组合需要在实际叠加背景上测试，不视为已经通过可访问性验证。

| 页面 / 状态 | 背景规则 |
|-------------|----------|
| 认证 | 白色为主，仅保留很淡的蓝粉色带，不制造登录大面板 |
| 发送 | 浅蓝主导，边缘留少量浅粉，输入区后方保持安静 |
| 接收 | 浅粉主导，边缘保留浅蓝，与发送共享同一背景连续切换 |
| 仪表盘 | 近白背景，弱化页面大色带，以具体块类型提供色彩变化 |
| 成功 | 仅当前块的状态图标和相邻反馈短暂使用浅绿，保留类型表面色 |
| 错误 | 当前操作附近使用深红文字或图标，背景保持原页面色，避免全屏报警 |

页面模式、文件类型、操作状态分别拥有颜色用途。状态通过独立图标和反馈表达，不覆盖块的类型表面色；不可仅凭颜色判断状态或格式。

### 7.3 排版与布局

| 项目 | 规范 |
|------|------|
| 字体 | 使用系统无衬线字体栈，中英文保持自然比例；key、源码、数值使用系统等宽字体 |
| 字号 | 使用语义化字号层级和相对单位；正文、元数据、类型标识、标题与品牌标识根据阅读距离、容器宽度和系统文字设置动态调整，不绑定固定像素 |
| 字重与字距 | 正文、控件、标题和类型标识使用清晰的层级字重；保持自然字距，不用压缩字距换取容量 |
| 行高 | 以可读性和内容密度动态设置相对行高；预览源码保留换行，并允许长行在合适的容器内滚动或换行 |
| 间距 | 使用可伸缩的设计令牌和容器间距函数；优先保持视觉节奏与可点击元素之间的呼吸感，不把间距锁定为固定像素序列 |
| 圆角 | 根据表面层级使用少量一致的圆角令牌；输入、内容块、选项面板和 Dialog 保持协调，小图标热点可圆形，不把所有元素做成胶囊 |
| 主输入区域 | 宽度由内容可读性、容器宽度和安全区动态决定；使用流式布局与约束函数，不使用固定绝对坐标 |
| 文本编辑区 | 根据内容、换行、设备方向、软键盘和可用视口动态扩展；达到当前容量后逐段展开，避免输入框内部滚动，不预设固定高度档位 |
| 发送 / 接收主块 | 采用响应式比例、最小可读内容空间和 `minmax` / `clamp` 等布局约束动态计算；类型图标和标签随容器密度调整，不依赖固定块宽高 |
| 超长文本输入 | 达到当前可用屏幕高度后，底部出现向下展开控件；每次点击沿页面文档流追加一段，仍有剩余则在新段末尾继续提供展开控件，不在输入框内纵向滚动；完整规则见 3.1.4 |
| 图标点击区域 | 满足平台和无障碍触控目标，并根据输入方式与可用空间动态扩大；视觉图标与可点击热点分离，hover 不改变点击区域大小 |
| 长文本 | key、文件名可换行或截断后完整查看；元数据不能覆盖预览或操作入口 |

顶部仅保留 Snipflow 标识及明确的“退出”入口；该入口只负责清除本地 Token 并返回认证页，不承担设置、账户管理或导航功能；发送 / 接收不增加导航栏。切换方向图标贴近可用视口两侧，并避让安全区，不紧贴浏览器返回手势边缘。

桌面与移动端维持同一功能模型。小屏预览可占据大部分视口，保留关闭入口及可滚动内容；软键盘弹出时主输入随正常布局移动，不让中心定位遮挡输入和完成操作。

### 7.4 文件类型与内容块

| 类型组 | 初始表面色 | 类型标识示例 |
|--------|------------|--------------|
| 普通文本 | block-text：#F1F0F5（浅珍珠灰） | TXT |
| Markdown / 文档 | block-document：#F8EFD8（浅香槟黄） | MD、PDF、DOCX |
| 图片 | block-image：#F9E2DC（浅珊瑚粉） | PNG、JPG、WebP |
| 结构化数据 / 源码 | block-code：#E2EFDE（浅鼠尾草绿） | JSON、XML、TS |
| 归档 / 自定义 | block-archive：#E0EFEC（浅青瓷色） | ZIP、7Z、自定义安全扩展名 |

以上 block-* 是独立的内容块表面令牌，不引用或别名映射到页面 blue-soft、pink-soft、green-soft。相同类型在发送、接收、仪表盘保持同一表面色，不随页面切换换色；细边框和低高度阴影辅助区分块体与背景。成功与错误仅改变局部状态图标及反馈，不覆盖类型表面色。初始色值仍待视觉确认与对比度验证。

具体类型通过图标加文本识别，不为每一种格式分配独立品牌色。扩展名标识长度受限，较长自定义类型显示通用文件标签，在详情中展示完整名称。

| 交互 | 外观与反馈 |
|------|------------|
| 静止 | 类型图标、标签和稳定块体，不含正文、文件名和图片内容 |
| 主区点击 | hover / focus 后显示详情意图；块体主体单击统一进入详情展开页，不直接触发快捷动作 |
| 快捷区点击 | 右下角固定小区域执行上下文主操作：发送、复制或下载；与主体详情事件严格隔离 |
| hover / focus | 块体以轻微、克制的位移或层次变化回应；详情意图与右下角快捷区在 hover、focus 或触屏可发现状态下显示，快捷区使用明确图标与可访问名称，不以隐藏的详情页选项入口承载主操作 |
| 按下 | 以短暂的压感、对比或阴影变化回应，不移动周围布局，不影响长按识别；反馈强度按设备和输入方式调整 |
| 块转换 | 文本与附件相互转换时更新适用的标签、图标和表面，不整页闪烁；附件成块后没有类型切换动效 |
| 详情内选项 | 在同一详情容器内展开，不锚定原块另开选项浮层；转换操作进入对应编辑界面，目标类型控件仅在文本转附件确认前出现 |
| 加载 | 在不改变块体布局的稳定区域显示进度图标与状态名称；不显示没有字节依据的百分比 |
| 过期 | 降低类型表面强调度并显示过期标识，保留可辨认文字和 key；详情主操作按既有过期规则处理 |

触屏长按与主体点击打开同一个详情容器；右下角快捷区直接执行发送、复制或下载，不打开详情或预览。预览直接位于详情内，选项在其内部展开，不依赖 hover 才能发现关键操作。转换编辑接替当前详情，删除确认仅让顶层可交互，不出现多个同时可操作的浮层。

### 7.5 关键动效

动效使用已选 Motion 与 CSS。单次过渡具有明确起止状态，可被新的用户操作安全打断；网络完成时间不受动画时长影响。

| 场景 | 交互节奏 | 动作 |
|------|----------|------|
| 图标、边框、hover | 即时、轻量 | 颜色、透明度与轻量 transform，不改变布局尺寸；避免让反馈慢于用户预期 |
| 详情内选项展开 | 短促、可逆 | 在详情容器内自然展开与收起，关闭略快；不另开浮层，不使用弹跳 |
| 文本确认成块 | 连续、可理解 | 输入内容与块体通过共享布局衔接，类型图标接续出现，不整页闪烁 |
| 重新编辑 | 平滑、保持上下文 | 块体展开回编辑区域，再恢复文本与输入焦点；不缩放变形正文 |
| 发送 / 接收切换 | 与手势方向一致 | 内容区沿切换方向轻移并交接淡入淡出，背景色带连续转场；导航与焦点在视觉过渡中保持可预测 |
| 认证 -> 功能页 | 先状态、后过渡 | 认证成功状态先确定，再开始页面过渡；目标页面从对应方向进入，失败时保留可修正反馈 |
| 预览 / 详情展开 | 稳定、聚焦内容 | 暗化背景，块结构展开居中显示，延迟一段时间后，如果预览块可以显示，则将详情块推入左边，并在右边展开预览窗；平滑动画 |
| 保存 / 复制成功 | 短暂确认、结果持久 | 当前反馈图标切换并短暂强调；发送凭证保持显示，不自动消失 |
| 错误 | 克制、可行动 | 边界及错误说明平滑出现，不抖动输入框，不循环闪烁；同时提供清晰的修正路径 |
| 瀑布流筛选 | 连续、稳定 | 可见条目位置平滑调整，进入和离开淡化，不改变排序语义，不制造跳跃 |
| 过期归零 | 状态转变明确 | 块表面与状态文字平滑切换，不自动移除或触发连续提示 |

位置与表面过渡初始采用 cubic-bezier(0.22, 1, 0.36, 1)。仅拖动取消可使用无明显过冲的弹簧回弹；不全局套用弹性动画。

首屏不做长开场动画，不逐字显示品牌或输入提示。瀑布流首批出现若使用错峰，应以设备性能和首屏可用性为依据控制节奏；后续大量条目不累计延迟，也不能让用户等待动画才能操作。

### 7.6 手势与背景变化

| 行为 | 自适应规则 |
|------|------------|
| 点击切换 | 边缘方向控件直接提交路由，使用统一页面过渡 |
| 水平手势识别 | 从允许的背景区域开始移动后，根据设备密度、可用宽度和方向置信度判断；水平分量明显大于垂直分量才接管，避免误吞滚动 |
| 提交阈值 | 根据可用宽度和当前输入方式采用相对阈值；阈值应让用户容易理解并可回弹，不以跨设备统一的固定像素值或速度分支决定 |
| 取消 | 未达阈值、pointercancel 或反向退出时复位，不修改路由 |
| 内容阻尼 | 页面主内容只随手势轻移，不整屏跟随指针，也不改变输入框尺寸 |
| 背景响应 | 拖动进度控制宽色带的位置与混色；取消回到原色，确认进入目标色 |
| 输入准备 / 请求中 | 当前内容附近色带完成一次轻量变化；请求中显示局部指示，不持续推移整幅背景 |
| 静止页面 | 背景默认静止，不配置无休止的全屏漂移动画 |

长按阈值和移动容差由统一手势原语根据设备与可用性测试动态调校；超过容差即取消长按候选，释放后不追加主操作 click。参数不能误吞页面滚动或文字选择。背景文件拖放仍按上传接入逻辑处理，不作为路由拖动。

### 7.7 预览与瀑布流

| 表面 | 设计规则 |
|------|----------|
| 图片预览 | object-fit: contain，展示完整图像；透明图可使用低对比棋盘底，不能裁剪代替适配 |
| Markdown 预览 | 阅读列宽度由详情容器和可读性动态决定，正文采用与全局排版协调的相对字号；标题保持文档层级，表格和代码块局部横向滚动 |
| 源码预览 | 使用等宽字体和稳定相对行高，不默认引入代码编辑器；换行和复制保留原文语义 |
| 元数据 | 名称和值对齐排列，长 key 可换行；不把每个字段装成独立卡片 |
| 详情与预览排布 | 同一模态层内，详情与预览是各有独立白色表面、圆角和边界的两块，中间保留响应式间隙；两块高度各自随内容决定，不拉伸等高。文本及 Markdown 预览按内容收缩，同时保留约 18rem 最小宽度和 12rem 最小高度，分别受容器宽度与可用视口约束，避免短内容形成过小碎片；图片按原始宽高比在宽高上限内等比缩放，图片区域贴合实际图像，方形图像保持方形，不用宽大空白填满预览上限。桌面详情在左、预览在右，详情宽度不随预览内容变化；预览按内容收缩，最大宽度约为详情的两倍，两块作为整体居中并沿垂直中心对齐；窄屏预览在上、详情在下，仍保留独立表面和间隙。无预览时详情单独居中；内容不溢出视口，关闭入口保持可达。发送详情的发送、替换为附件和重新编辑操作保持单行，通过紧凑间距与字号适配，小容器可省略装饰图标，保留完整文字。两块可独立变换，展开延迟与平移动画在阶段十调校 |
| 桌面详情 | 内容宽度和高度根据可用视口、预览类型与阅读需要动态约束，内部滚动但保持关闭入口可达 |
| 移动详情 | 视口宽度减两侧安全留白，高度受可用视口限制；关闭入口固定可达，不与内容重叠 |
| 背景遮罩 | 轻度中性暗化，不做重度全屏模糊；遮罩点击只关闭顶层详情 |
| 瀑布流 | 内容宽度、列数和间距由容器查询、可用空间与内容密度动态决定；窄屏自然收敛为易读列数 |
| 块高 | 采用由内容比例和布局条件计算的稳定尺寸；同 key 在同一布局条件下保持可预测，不用随机数造成每次刷新跳动 |
| 搜索与倒计时 | 搜索栏位置稳定；倒计时使用等宽数值并预留稳定的呈现空间，文字变化不推动其他块 |

详情内部不放第二层装饰卡片；内容块的布局扩展只发生在独立详情层，不撑开原瀑布流。关闭后恢复原滚动位置，条目已被删除则按第 5 章恢复可用焦点。

### 7.8 统一内容块交互

统一采用“主体点击或长按进入详情、右下角直接执行上下文快捷操作”。详情与适用预览共用一个模态层和焦点边界，但以有间隙、可独立运动的两个内容块展示；元数据与可展开选项位于详情块，不另开预览模态层或块旁选项浮层。主体与快捷区分别可聚焦，事件不得互相冒泡；长按不追加 click，Esc 或遮罩关闭后恢复焦点。详情按内容能力居中或向左展开，使用中性遮罩并恢复原位置与滚动锚点。具体页面动作遵循 3.1.3、3.2.1 和 5.3 的上下文契约。

所有按钮、图标热点和可触发区域都遵循统一的反馈状态：待机时有明确 affordance，hover / focus 时提升可发现性，按下时即时回应，请求中显示局部进行中状态，成功或失败后保留可理解的结果与下一步。反馈不得改变周围布局或让同一触发重复执行；键盘 Enter / Space、触摸和指针触发使用同一语义回调。不可用状态应说明原因或等待条件，不能只降低透明度。任何过渡都服务于状态理解，不得延迟实际操作、遮挡焦点或阻止连续操作。

### 7.9 简化与验收

| 条件 / 场景 | 规则或验收目标 |
|-------------|----------------|
| reduced-motion | 取消位移、缩放、错峰、回弹和背景移动，只保留必要短淡入或直接更新；状态信息不减少 |
| 标签页隐藏 | 暂停视觉循环，不影响第 6 章的 Token 续期语义 |
| 性能不足 | 先移除背景过渡、阴影动画和复杂共享布局，保留图标、文字及直接操作 |
| 响应式检查 | 至少覆盖窄屏、平板、桌面和超宽屏等代表性容器，并使用不同方向、缩放级别与软键盘状态检查无横向溢出；不把某组固定像素视口当作唯一合格标准 |
| 文字压力 | 长中文文件名、128 字符 key、较大数字和错误提示不覆盖按钮或相邻内容 |
| 输入可用性 | 软键盘、粘贴文件、文本选择、背景滑动互不冲突；长文本逐段展开使用页面滚动，未展开部分仍完整参与确认和发送 |
| 类型配色 | 三页同类型块颜色一致；块表面不引用页面主色令牌，成功状态不覆盖类型表面色 |
| 动效真实性 | 请求未成功不显示成功，失败不因动画结束而消失，未测字节进度不展示精确百分比 |
| 可访问性 | 焦点可见、必要对比达标、键盘可完成所有核心操作，触屏不依赖 hover |
| 预览 | PNG / JPG 不失真裁切，MD 可读，具体类型标签正确，未知类型有摘要回退，远端资源不自动加载 |

首版在浏览器报告不超过 2 个逻辑核心或不超过 2 GiB deviceMemory 时保守启用简化动效，未提供硬件提示的浏览器遵循系统 reduced-motion；不把这些提示当作精确性能测量。简化模式停用共享布局和背景 / 阴影过渡，保留短淡入、焦点及业务反馈。

## 8. Roadmap

以下阶段从当前已有的 React / Vite P0 工程继续实现，不重新生成项目。阶段一核验并补齐现有骨架，阶段四即通过真实 Worker 完成认证、文本发送、接收和删除的最小闭环；随后在同一条链路上扩展附件、转换与仪表盘。每个阶段只列出本阶段需要建立的边界、产物和验证场景；字段规则、交互参数和视觉自适应规则以引用章节为来源，不在 Roadmap 中复制维护。

按阶段依赖推进，每阶段都保留此前已通过的可运行链路。测试随对应功能建立，真实接口验证从阶段二开始，不集中拖到发布前。复杂能力按需扩展，不要求最小闭环预先完成全部预览、分页和缓存预算；已经启用的功能仍必须遵守协议、认证和错误边界。发现设计需要变化时，先修改第 1–7 章的来源章节，再同步相关实现与测试。具体文件拆分、辅助接口及内部算法由开发者在这些边界内选择。

### 8.1 阅读路径与阶段依赖

开发者不需要在每个阶段重读全文。先阅读本节和当前阶段列出的“聚焦章节”，遇到跨层问题再按下表回到唯一来源。

| 设计域 | 唯一来源 | 首次落地阶段 | 后续消费者 |
|--------|----------|--------------|------------|
| 技术栈、工程约束 | 1、2、5.1 | 阶段一 | 全部阶段 |
| 原始对象协议、错误语义 | 4.1–4.7 | 阶段二 | 发送、接收、仪表盘 |
| 类型、内容角色与预览能力 | 3.1.3、4.3–4.5 | 阶段二、五 | 三个功能页面 |
| 状态所有权与异步身份 | 5.2、5.4–5.7 | 阶段三、四；阶段八扩展缓存 | 所有 feature |
| 认证会话 | 3.1.1、6 | 阶段三 | API、Query 和页面清理 |
| 公共内容块与详情 | 3.1.3、3.3.4、5.3 | 阶段四建立文本基础，阶段五扩展 | 发送、接收、仪表盘 |
| 页面业务流程 | 3.1、3.2、3.3 | 阶段四最小闭环，阶段五至九扩展 | 页面组合与 E2E |
| 视觉、手势与无障碍 | 7 | 阶段一建立令牌，阶段十收口 | 所有组件 |
| 分层验收 | 4.8、5.9、6.5、7.9 | 各相关阶段 | 阶段十一总验收 |

依赖主线如下；后续阶段可以复用已通过的产物，不得绕过它们建立第二套实现。

```text
现有工程核验与验证基线
  -> domain / api 协议边界 + 真实 Worker 冒烟验证
  -> 认证会话与路由壳
  -> 文本发送、接收、删除的真实最小闭环
  -> 公共内容块、普通附件与安全预览
  -> 完整文本编辑与双向块转换（含 raw 解释）
  -> 发送选项、冲突与接收交互补全
  -> 分页快照、缓存与资源预算 -> 仪表盘
  -> 跨页面手势、视觉和无障碍收口
  -> 全量真实 Worker 回归与发布
```

贯穿全部阶段的五条门禁：

1. 上传正文只存在一份权威字节表示；控制 JSON、索引 JSON 和对象 Blob 必须走不同解析分支，见 4.1、4.6。
2. `fileType`、`contentRole`、`previewKind` 分别表达类型、操作和预览能力；页面不得重新发明 `text / image / file` 三类枚举，见 4.3.2。
3. 路由、认证、草稿、服务端缓存和局部交互按 5.2 各有唯一所有者；Token 不进入 Query key、URL、日志或 Zustand 持久化。
4. 每个异步结果先核对 `sessionId`，草稿相关操作再核对 `draftId / revision / operationId`；过期回调不得改写当前界面，见 5.4–5.6。
5. 网络结果驱动业务状态，动效只订阅状态；POST、DELETE 和结果未知的写操作不得自动重试，见 4.7、5.8。

---

### 阶段一：整理现有 P0 工程并补齐开发基线

目标：以现有代码和 lockfile 为起点，将模板整理为可持续扩展的 Page 工程，保留已经配置的构建能力。

聚焦章节：1、2、5.1、7.2–7.3。

实现步骤：

1. 使用现有 package.json 与 lockfile 安装并运行 dev、build、lint，记录实际基线；保留 Vite 端口和已接入的 React Compiler，不运行脚手架重建项目。按第 2 章的版本选择策略核对兼容性，必要调整同时更新依赖及 lockfile。
2. 开启 strict、noUncheckedIndexedAccess、exactOptionalPropertyTypes 并修正当前代码。保留已有脚本，补齐 format、typecheck、test 和最小单元 / 组件测试环境；后续 MSW、Playwright、axe 随对应阶段接入。检查声明的 lint 规则确实进入执行链。
3. 整理 main.tsx、App.tsx 和模板样式，移除不再使用的模板展示及其引用。按 5.1 建立最少应用入口，接入 Router、QueryClient、错误边界；Zustand 及 feature 在首次使用时加入，不预建全部空目录。
4. 建立 `/auth`、`/send`、`/receive`、`/dashboard` 与 `/` 的可渲染骨架，支持刷新和前进后退。认证与业务在后续阶段接入，占位页面不模拟发送成功。
5. 接入所需样式依赖，将第 7 章基础色彩、间距、排版和焦点样式集中管理，替换模板默认样式；本阶段不要求完成内容块造型与复杂动效。
6. 在现有 vite.config.ts 中配置 `/health`、`/snip`、`/stats` API 代理，目标使用实际 Worker 地址，保留完整路径和 Header；Token 由运行时提供。将模板 README 改为本项目启动、检查和联调说明，明确 Worker 为外部前置服务及生产同源要求。

退出条件：

```text
开发服务可打开全部路由，刷新不返回 404
严格类型检查、lint、格式检查、最小组件测试和生产构建通过
路由骨架没有第二份 currentPage 状态
构建产物及配置中不包含开发 Token 或固定 Worker 凭据
```

---

### 阶段二：固定领域模型与 HTTP 协议边界

目标：先得到可独立测试的 `domain` 和 `api`，让所有页面共享同一套类型、Header、响应分流和错误语义。

聚焦章节：4.1–4.7；类型能力另读 4.3.2、4.4–4.5；分层限制读 5.1。

实现步骤：

7. 在 `domain` 定义 `SnipIndex`、`CreateSnipResponse`、列表与统计 DTO、对象响应模型、统一 API error，以及发送草稿的内容与选项类型。可选字段和空值必须与 4.6 一致。
8. 建立唯一文件类型定义表，集中维护 id、标签、扩展名、MIME、类型组、默认 `previewKind` 和转换前目标约束；另用纯函数从 MIME、文件名、下载语义和检测证据派生 `fileType / contentRole / previewKind`。
9. 实现 key、TTL、MIME、日期、字节大小、安全文件名和响应头解析。非 ASCII 文件名只在构造 `X-Snip-Filename` 时编码一次；随机 key 与永久 TTL 通过省略 Header 表达。
10. 用 Zod 校验认证、创建、列表和统计等控制 JSON；未知响应字段可忽略但不依赖。对象 GET 只检查状态与响应头并读取 Blob，不调用 `response.json()`。
11. 建立统一 API client：接收 `AbortSignal`，运行时注入 Bearer Token，区分 204、控制 JSON、原始对象和异常响应；`cache: no-store`、401 回调及 requestId 提取在此集中处理。
12. 实现 create、list、read、delete、stats 和 auth handshake 调用。create 直接传入原文 UTF-8 字节或 File / Blob，禁止 JSON 外层、multipart、二次 Base64 和手动 `Content-Length`。
13. 建立 MSW fixture：至少包含文本、JSON 对象正文、图片、未知二进制、非 ASCII 文件名、filename 缺失、永久与过期索引、分页 cursor、204、主要 4xx、5xx、网络中断和控制端点 200 HTML。

使用独立测试对象，通过阶段一开发代理对真实 Worker 执行认证、创建、读取和删除的 API 冒烟验证，确认原始字节、状态码与 Header；本阶段可使用临时测试入口，不等待完整页面。Mock 用于可重复的错误验证，不能代替真实协议验证；若 Worker 尚不可用，明确保留联调未通过的状态。

退出条件：

```text
任意二进制 POST 的 Request body 与输入字节一致
GET JSON/HTML 用户对象仍返回 Blob，不被识别成控制响应
控制 JSON 非法、对象读取失败和 204 分支可明确区分
类型、文件名、TTL 和 Header 纯函数覆盖 4.8 的协议边界
api/domain 不导入 React、Router、Zustand 或页面组件
真实 Worker 的认证与对象创建、读取、删除契约经开发代理验证
```

---

### 阶段三：认证会话、路由守卫与应用级清理

目标：建立所有远端状态的认证边界，使后续功能天然按会话隔离，而不是在各页面分别处理 Token 和 401。

聚焦章节：3.1.1、5.2–5.3、6.1–6.5；页面切换外壳读 3.1.2。

实现步骤：

14. 在 auth 模块实现 `snipflow.auth` 的解析、异常清理和 `VITE_AUTH_IDLE_TTL_SECONDS` 校验；Token 无法持久化时保留明确的仅内存会话模式。
15. 每次新认证生成不含 Token 信息的随机 `sessionId`。向 API client 提供当前凭据读取器和统一 401 处理器，而不是由组件拼接 Authorization。
16. 实现手动 `/health/auth` 验证、有效缓存直达、原目标恢复和根路由重定向。网络错误不等同于凭据错误，错误 Token 不覆盖仍有效的缓存。
17. 实现功能页进入与每 30 秒的本地续期、`pagehide` 离开时间写入、`storage` 跨标签页同步及配置为 0 的永不过期分支；页面切换不得重复握手。
18. 建立唯一 `endSession` 协调动作：主动退出或当前会话的 401 时停止续期、清理已接入的认证及页面资源，再导航 `/auth`。后续草稿、Query、预览 URL 和 Worker 在引入时接入同一清理入口；迟到回调用旧 `sessionId` 自动失效，不要求本阶段预建尚未使用的资源模块。
19. 实现 AuthGate 与 TransferShell 的结构和可访问方向入口；本阶段只需点击切换，拖动、背景插值和共享元素转场留到阶段十。
20. 确定主动退出与强制会话结束的边界；阶段四接入草稿后补上未提交或发送中退出的一次确认。401 清理不能等待用户确认，也不得建立草稿恢复或远端删除流程。

退出条件：

```text
覆盖 6.5 的首次验证、重开、闲置、持续停留、后台恢复和永不过期场景
/send、/receive、/dashboard 的直接访问能恢复原目标
发送/接收切换不重新认证，仪表盘不出现在切换序列或导航入口
退出、跨标签页换 Token 和 401 后旧请求不能写入新会话
Token 不出现在 URL、Query key、日志、构建产物或业务 store
```

---

### 阶段四：真实文本发送、接收与删除的最小闭环

目标：在现有认证与 API 基础上，尽早得到浏览器内可用的真实链路。使用最终架构的基础实现，后续直接扩展；本阶段不依赖转换前类型设置、安全预览、双向块转换、仪表盘或复杂动效。

聚焦章节：3.1.3–3.1.4、3.1.6、3.2、4.2、4.6–4.7、5.2、5.4–5.6。

实现步骤：

21. 接入 send / receive store 与最少 Query / mutation。草稿保存在 send store，接收正文只保存在 Query；建立 sessionId 和草稿操作身份检查、按 key 的读取去重及缓存失效，禁止自动重试写请求。
22. 实现普通文本输入、完成确认和只读文本块，以默认随机 key、24 小时有效期发送。使用公共 ContentBlock 和基础 DetailDialog，保持主体打开详情、快捷区执行发送或复制；先支持普通输入，完整分段展开在阶段六完成。
23. 提交时冻结当前正文和选项、锁定草稿；201 且响应有效才显示服务端 key。401 结束会话，明确拒绝保留草稿，网络中断或不可确认结果进入 uncertain 并提示可能已保存；禁止自动重发。成功后由新建动作恢复默认草稿。
24. 在接收页输入 key，真实获取 Blob 并安全解码文本；显示只读详情、提供完整原文复制。保留输入 key，区分不存在、网络失败与读取失败；新查询或换会话后旧响应不能替换当前结果。
25. 接入详情内删除确认；204 后清除相关缓存并返回输入，404 同步目标不存在，失败或结果未知保留真实状态。草稿保留、退出确认及 endSession 清理覆盖本阶段新增资源。
26. 经开发代理连接真实 Worker，在浏览器完成认证 -> 文本输入确认 -> 发送 -> 复制 key -> 接收 -> 复制正文 -> 删除 -> 再读不存在。验证 UTF-8、换行、空白与默认 Header，联调使用独立测试对象。
27. 建立这条链路的自动化回归，并用 MSW 验证拒绝、响应丢失、重复提交和迟到响应。此后每阶段都重新验证该链路；可以临时使用简单布局，但不能通过模拟成功跳过真实接口退出条件。

退出条件：

```text
真实 Worker 上可从认证完成文本发送、接收、复制和删除，无需仪表盘或 raw 功能
普通文本原样往返，准备阶段不上传，重复点击不重复提交
未确认结果不显示 sent，不自动重发；401 与换会话清理生效
文本块主体与快捷动作分离，新增功能沿用同一 API、Query、store 与公共组件
最小浏览器流程和关键错误回归通过，后续阶段持续保留
```

---

### 阶段五：公共内容块、普通附件收发与安全预览

目标：扩展阶段四的公共内容块与详情，让选择、拖入或粘贴的普通文件沿用同一提交和接收链路，并建立按能力安全预览的规则。

聚焦章节：3.1.3–3.1.4、3.2、3.3.4、4.2–4.5、5.3、5.7、7.4、7.7–7.8。

实现步骤：

28. 扩展已有 ContentBlock 的稳定尺寸、类型图标、类型标签与状态。主体和可选快捷区继续为两个同级交互区域，不嵌套按钮、不在组件内部调用 API；为 dashboard 保留仅打开详情的上下文。
29. 通过选择、拖入和粘贴接收一个 File / Blob；同次粘贴优先文件，已有草稿需要明确替换。本地大小、名称和基本类型校验通过才成块，最终发送沿用阶段四 mutation，以原字节及文件名 Header 上传，不提前上传。
30. 扩展接收分类、BlockActions 与 DetailDialog：无名可安全解码正文复制，有名附件、图片和未知二进制下载；详情中提供摘要及适用操作，删除沿用已有确认流程。接入 Object URL 的创建、延迟下载释放和会话清理。
31. 实现 PreviewSurface 的普通文本、raster、Markdown、源码和 metadata-only renderer。可预览的普通文本与源码正文必须在详情的专用 PreviewSurface 区域内展示，与元数据及操作区分离，不继续作为详情顶部散落的裸 `<pre>`；按 4.4 执行像素、字节、节点、嵌套与协议限制，HTML、SVG 和脚本只作源码，不自动加载远端资源。
32. 详情层组合已有索引与按需 Blob；读取或预览失败仍保留元数据和下载。关闭、Esc、遮罩、删除后焦点恢复遵循 3.3.4 和 5.8。
33. 建立 pointer/click 抑制的公共交互原语，使长按、拖动、文字选择、快捷区和主体事件互斥；完整阈值和动画在阶段十调校。

通过真实 Worker 验证至少一个图片、一个文本附件和一个未知二进制的发送、读取、下载与删除；比较原始字节和文件名，验证预览失败仍可下载。

退出条件：

```text
主体只打开详情，快捷区只触发上下文动作，事件不互相冒泡
键盘可以独立到达主体、快捷区、详情内容和关闭入口
同一 fileType 在三个上下文使用相同标签、图标和 block-* 表面色
MD 可在渲染与源码间切换；复制/下载始终使用完整原始内容
未知类型或预览失败仍可显示摘要并下载，不执行不可信内容
普通附件已接入真实收发链路，与普通文本共用提交、错误和会话边界
```

---

### 阶段六：完整文本编辑与双向块转换

目标：在已可发送的文本和附件草稿上补齐文本转附件、附件转文本两种操作。目标类型和 raw 解释方式在转换前设置，并确认附件名称；直接导入和转换所得附件使用同一模型，成块后类型只读。本地转换与最终发送仍是两个明确动作。

聚焦章节：3.1.4–3.1.5、3.1.7、4.2.1、4.4–4.5、5.4、5.7；输入布局读 7.3。

实现步骤：

34. 扩展已有 send store 的 `editing / preparing / converting / ready` 状态及可选转换前原文，继续只保存单一草稿；所有附件使用同一模型与转文本操作，不另建来源子类型或 raw 提交链路。双向转换保留 key / TTL，文本不沿用附件名称和 MIME。
35. 补齐完整原文驱动的多档输入、逐段展开和重新编辑，先验证长文本、选区及软键盘的可用性，再接入收拢表现；展示变化不切分、裁剪或改写正文。
36. 在文本转附件的编辑界面实现 FileTypeSelect、文件名及解释方式，确认前校验目标与字节的兼容性。已有附件块不提供类型或 MIME 修改入口；直接文件导入复用阶段五能力，识别不出类型统一显示未知。
37. 在浏览器 Worker 中实现带 2 秒上限的 raw 候选检测；使用 `sessionId / draftId / revision` 回收旧结果，同一未变原文被拒绝后不重复弹窗。
38. 实现文本转附件的 UTF-8 原文、Base64、Base64 Data URL 和 hex 解释；确认前显示类型、名称、解释方式和输出大小。实现同一个附件转文本操作：有转换前原文则恢复，否则按 4.4 解码 Blob，二进制也允许转换。失败、取消和超限保留操作前草稿。
39. 文本转附件确认时一次 action 生成 Blob、名称、MIME、类型和预览版本；附件转文本进入编辑并释放旧预览。转换不合预期时先转回文本再重新设置参数，禁止直接改附件类型；格式冲突不能靠改后缀伪装。完整发送选项在阶段七接入。
40. 组合公共详情、适用预览、内部选项和双向转换入口，按 5.4 限定可修改阶段；所有准备行为保持本地，不提前 POST。真实验证原文转附件、raw 解码转附件及附件转文本后发送，分别按当前文本或字节核对结果，不把强制二进制转文本当作无损往返。

退出条件：

```text
文本、File/Blob 和 raw 转换都只形成一个 ready 块，准备过程不上传，最终发送复用已有链路
超长输入始终以完整原文参与检测、转换、计量和后续发送
旧检测结果、失败解码和取消任务不能替换当前 revision
仅转换前可选目标类型，附件成块后只读；转换后标签、MIME、文件名、字节与预览同步一致
直接导入和文本生成的附件具有相同转文本能力；原文恢复与 Blob 解码属于同一个反向操作
覆盖 4.4 支持及拒绝边界，失败保留操作前内容；主动二进制转文本允许替换字符，不承诺无损恢复
```

---

### 阶段七：发送选项、冲突处理与接收交互补全

目标：在真实收发与三条准备路径上补齐选项、冲突和异常操作体验，完成发送与接收页面；不重新实现提交协调层。

聚焦章节：3.1.6–3.1.7、3.2、4.2、4.6–4.7、5.4–5.6。

实现步骤：

41. 在已有冻结提交机制上接入完整发送选项，确认三条准备路径均遵守内容与选项锁定、跨页保留和身份检查；路由切换不把已发出的写请求解释为撤销。
42. 构造 POST：文本用 UTF-8 原字节，附件用原 Blob；自定义 key、TTL、filename 和 overwrite 严格按 4.2 省略或编码，source 固定为 page。
43. 沿用 201 且响应 Schema 有效才进入 sent 的规则，展示服务端 key、保留创建响应元数据并失效同 key 旧正文；新建动作才清除已提交块并恢复默认选项。全量索引更新在阶段八接入，不为尚未进入的仪表盘提前请求列表。
44. 409 进入 conflict，只允许修改 key 或经二次确认用同一冻结内容发送 overwrite=true；任何其他路径不得自动覆盖。
45. 当前会话的 401 结束认证，409 按冲突处理，其余明确拒绝进入可修正的 failed；网络中断、主动取消、5xx 或响应解析失败按 4.7 判断为 uncertain，提示可能已保存且不自动重发。补齐各状态的提示和后续操作，保留草稿。
46. 如果回调对应旧草稿，只更新仍有效的操作记录，不替换当前块；如果认证会话已变化，完全丢弃结果。

补齐接收页的背景返回、Esc 和顶层弹层关闭优先级；复制、下载不自动返回，删除成功或 404 清理过时引用，普通失败或 uncertain 不伪造已删除。缺失元数据的分页补取在阶段八接入。通过真实 Worker 验证自定义 key、四种 TTL 与明确覆盖，并持续回归文本、附件及 raw 链路。

退出条件：

```text
三条准备路径均以原字节完成 POST，Header 与冻结选项一致
重复点击只产生一次请求，动画结束不会制造 sent
409 未经确认不带 overwrite=true；未知结果不自动重试
发送成功保留可供后续索引更新的元数据，失效旧正文，不主动请求列表或统计
失败、冲突和未知结果均保留草稿及可理解的下一步
接收主体、快捷动作、弹层关闭和背景返回互不串联，删除失败不制造成功状态
```

---

### 阶段八：分页快照、缓存一致性与资源预算

目标：在已有收发链路及真实资源使用方式上，补齐仪表盘所需的快照和缓存能力，统一处理跨页写入、分页竞争及资源预算。

聚焦章节：3.3.2–3.3.3、4.7、5.2、5.4–5.8。

实现步骤：

47. 在已有正文 Query 和 mutation 基础上补齐 5.6 的全量快照、统计及局部 metadata 查询；全部按 sessionId 隔离，明确禁用聚焦、重连、重新挂载、定时器及默认重试引发的隐式网络请求。
48. 实现完整索引快照加载器：cursor 循环保护、按 key 去重、完整度标记、generation 隔离、旧快照失败保留，以及加载期间本地变更覆盖层重放。
49. 扩展创建、覆盖、删除后的统一缓存更新：更新已知索引并失效对应正文，清除已删 metadata，统计标为待刷新；不得猜测覆盖后的 count。所有更新继续通过当前会话身份检查。
50. 接收详情在用户明确要求且元数据缺失时按 4.3.3 分页补取；与仪表盘复用读取能力，局部结果不冒充完整快照，补取失败不影响已有正文。
51. 在已有 Object URL 与任务清理基础上加入共享时钟、正文缓存字节预算和未活动条目的 LRU 驱逐。活动订阅不被一个面板的关闭误取消；下载、预览和 Worker 任务各自保持明确释放路径。
52. 结合已完成的发送和接收验证快速换 key、刷新中创建 / 覆盖 / 删除、重复刷新、缓存压力和换会话迟到响应；缓存扩展不能让原有收发流程产生额外请求或第二份正文。

退出条件：

```text
分页重复 cursor、失败和取消均不会发布伪完整快照，刷新失败保留旧数据
刷新期间的写入不会被旧分页结果覆盖或复活，统计只标记待刷新
局部 metadata 补取与完整快照语义分离，失败不阻断已有正文操作
同一对象的 Blob 只保存在 Query，去重、活动订阅及预算驱逐协同工作
Object URL、任务和缓存清理经卸载及资源压力场景验证
此前的真实文本和附件收发链路继续通过
```

---

### 阶段九：仪表盘快照、搜索与瀑布流

目标：以完整元数据快照驱动确定性瀑布流，正文只在用户打开详情时读取。

聚焦章节：3.3、4.3.3、4.6–4.7、5.5–5.6、5.8、7.7。

实现步骤：

53. 首次进入并行启动阶段八的索引快照和统计读取；索引按 cursor 渐进展示并标明完整度，统计始终展示服务端快照时间和待刷新状态。
54. 在当前已获取 key 上执行区分大小写的本地包含搜索。加载未完成时明确范围；清空搜索恢复同一快照，不发网络搜索请求。
55. 手动刷新使用新 generation 构建候选快照，完成后发布；失败保留旧快照。禁用轮询以及聚焦、重连、重新挂载和倒计时触发的隐式刷新。
56. 实现稳定排序、确定性块高和与 DOM/键盘顺序一致的响应式瀑布流；筛选和刷新不得用随机尺寸造成跳动。
57. 主体打开公共详情时才按 key 读取 Blob；关闭恢复滚动与焦点。搜索后源块消失或详情中删除时，焦点按 5.8 转移到可用条目或列表标题。
58. 用单个共享时钟从 `expiresAt` 重新计算剩余时间；到期只改变本地展示和内容可用性，不删除条目、不更新统计、不发请求。
59. 接入发送、覆盖和删除成功的已知变更层；列表不能显示 source 或 custom metadata，只有本会话创建响应可暂时补充 source。

退出条件：

```text
分页重复 cursor、部分失败、取消和刷新竞争均不会产生伪完整快照
搜索全程不请求服务端，正文不会随列表批量下载
刷新中发生创建/覆盖/删除后，旧分页结果不能复活过时条目
倒计时在后台恢复时按当前时间重算，且不会伪造服务端清理或统计变化
从窄屏单列到桌面多列的视觉顺序、DOM 顺序和焦点顺序一致
```

---

### 阶段十：跨页面交互、视觉动效与无障碍收口

目标：在业务状态已经稳定后完成手势、共享转场和视觉参数；本阶段只增强表现，不改变协议、状态所有权或操作语义。基础语义 HTML、可访问名称和焦点能力不能推迟到本阶段才补。

聚焦章节：3.1.2、3.2.2、5.8、7.1–7.9。

实现步骤：

60. 完成发送在左、接收在右的 TransferShell 转场和边缘方向控件；路由前进后退复用过渡，提交后聚焦目标，不重复压入历史记录。
61. 补齐文件拖入的即时反馈：进入发送页显示明确接收区域与“松开添加”文字；已有草稿提示需要确认替换；多文件、目录、超限和锁定状态给出不可接收原因。嵌套元素间移动不闪烁，离开窗口、取消或放下后清理提示；阻止浏览器默认打开文件，普通文字拖动不触发文件提示。用可访问状态消息配合视觉反馈，覆盖桌面拖入、拖出、取消、替换和失败浏览器测试。建立统一 pointer 手势协调器：只从允许的空白背景识别水平拖动，排除输入、块、弹层、滚动区、文件拖入和浏览器边缘；未达阈值只回弹不导航。
62. 调校长按、移动取消、释放抑制、文字选择和弹层事件层级，确保一次触摸最多产生一个业务动作。所有阈值只在统一原语维护。
63. 按 7.5 完成输入成块、详情展开、认证进入、筛选和反馈动画；网络状态是动画输入，任何 `animationend` 都不得提交业务结果。
64. 完成 7.1–7.7 的响应式布局、色带、内容块、预览和瀑布流视觉；类型色不随页面变化，成功或错误只改变局部状态表达。
65. 实现 `prefers-reduced-motion`、标签页隐藏降频和性能降级。关闭动效后保留相同的路由、焦点、反馈和操作能力。
66. 用键盘、触屏和辅助技术检查名称、焦点陷阱与恢复、平台触控目标、对比度、live feedback 和删除后焦点；不依赖 hover、颜色或动画传达核心信息。
67. 建立窄屏、平板、桌面和超宽屏等代表性容器的视觉回归场景，并覆盖不同缩放、方向、长中文文件名、128 字符 key、大数值、软键盘、超长输入和详情内部滚动。

退出条件：

```text
点击、拖动、长按、文件拖入、页面滚动和文字选择之间没有串行动作
reduced-motion 下完整 E2E 仍通过，业务结果不依赖动画完成
三页同类型块的标签和表面色一致，状态色不覆盖类型色
键盘无需手势即可完成认证、发送、接收、详情、复制/下载和删除
7.9 的尺寸、文字压力、输入、预览、性能和可访问性场景通过
```

---

### 阶段十一：全链路回归、预发布验证与发布

目标：在此前持续真实联调的基础上验证完整系统，并在开发代理、预发布和生产同源环境完成相同主链路；此阶段进行总回归，不作为首次真实 Worker 联调。

聚焦章节：1；4.8、5.9、6.5–6.6、7.9；接口细节在失败时回看 4.2、4.6–4.7。

实现步骤：

68. 固化分层测试套件：domain 纯函数、api 响应分流、组件事件边界、页面状态、MSW 与 Query/Zustand 集成、Playwright E2E、axe 和视觉回归。每类测试放在最接近其责任边界的位置。
69. 运行完整静态与构建检查，修复所有类型、lint、格式、未处理 Promise 和资源泄漏问题；检查生产包不包含 Token、fixture 正文或仅开发使用的绝对 API 地址。
70. 经 Vite 代理连接真实 Worker，分别验证无名文本、MD 附件、PNG/JPEG、JSON、未知二进制、非 ASCII 文件名、随机与自定义 key、四种 TTL 和明确覆盖。
71. 对每个对象执行 create -> read -> dashboard detail -> download/copy -> delete；逐字节比较上传前输入与下载 Blob，分别核对创建 JSON、列表 JSON 和对象响应头的字段来源。
72. 注入 400、401、404、409、413、415、5xx、网络中断、响应丢失和控制端点异常 HTML；确认草稿、认证清理、缓存更新与 uncertain 状态均符合 4.7。
73. 执行并发与资源压力测试：快速换 key、刷新中写入、多标签页退出、预览反复开关、正文缓存超过预算、后台暂停恢复及会话切换后的迟到响应。
74. 在预发布使用独立 Token 和测试对象验证真实 Header、cursor、统计、TTL 到期与删除；若 Worker 开启伪装模式，明确记录 4.7 的读取歧义，不在 Page 增加正文猜错逻辑。
75. 部署时令 Page 与 Worker 同源，确认所有运行时 API 请求仍为相对地址且带 Bearer Token；重新执行浏览器主链路并清理测试对象。

发布门禁：

```text
pnpm typecheck
pnpm lint
pnpm exec prettier --check .
pnpm test
pnpm build
pnpm check:build
pnpm deploy:check
pnpm exec playwright test
pnpm test:e2e:production
git diff --check
```

真实接口测试由运行时环境提供 Token；没有 Token 的跳过结果不算真实联调通过。生产产物全量回归另以 SNIPFLOW_E2E_REDUCED=1 验证；最终线上 origin 通过 SNIPFLOW_E2E_BASE_URL 指定，配置与外部门禁见 6.6 和 deploy/README.md。

最终验收矩阵：

| 维度 | 必须成立 |
|------|----------|
| 架构 | 页面、feature、公共组件、domain、api、query 和 store 没有越过 5.1 的职责边界 |
| 协议 | POST 是原始正文，GET 对象是 Blob，只有控制端点解析 JSON；文件名、TTL 与 overwrite Header 规则一致 |
| 状态 | 当前路由、Token、草稿、索引和局部交互各有唯一所有者；所有迟到异步结果受身份门禁约束 |
| 交互 | 三页统一主体或长按打开详情；发送 / 接收的右下角执行快捷动作，仪表盘不设快捷区。预览和选项位于同一详情，发送准备、接收返回和弹层关闭互不串联 |
| 块转换 | 仅文本转附件与附件转文本两种操作；附件来源不产生子类型，类型只在转换前设置，成块后只读；恢复原文与二进制解码语义遵循 4.4 |
| 安全与回退 | 不执行附件或不可信 HTML，不自动加载预览外链；未知类型、预览失败和网络异常仍保留原始内容操作 |
| 认证 | 默认闲置期限、持续停留续期、配置为 0、跨标签页同步、主动退出和 401 清理全部符合第 6 章 |
| 生命周期 | Blob、Object URL、浏览器 Worker、请求订阅和缓存预算在替换、关闭、删除与换会话后正确收敛 |
| 发布 | 开发代理与生产同源均通过 Token -> 发送 -> 接收 -> 仪表盘 -> 删除主链路，运行时不依赖开发代理配置 |
